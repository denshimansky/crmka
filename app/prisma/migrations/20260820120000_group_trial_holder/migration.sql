-- Группа-держатель индивидуального пробного (пробного без группы).
-- ЗП инструктора начисляется только через Attendance у Lesson, поэтому
-- индивидуальному пробному теперь заводится собственное техническое занятие в
-- скрытой группе. Флаг отличает её от группы разового занятия (is_one_time):
-- держатель дополнительно прячется из сетки расписания, где пробное рисуется
-- своей карточкой. Аддитивно, бэкфилл не требуется — существующие пробные
-- получают занятие лениво, при отметке (PATCH /api/trial-lessons/[id]).
ALTER TABLE "groups" ADD COLUMN "is_trial_holder" BOOLEAN NOT NULL DEFAULT false;
