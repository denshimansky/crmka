-- Дата, с которой действует оклад сотрудника. NULL → оклад за весь месяц во всех
-- периодах (обратная совместимость для действующих окладников). Иначе оклад не
-- начисляется за месяцы до неё, а первый неполный месяц — пропорционально
-- календарным дням (см. app/src/lib/salary/oklad-for-period.ts).
ALTER TABLE "employees" ADD COLUMN "oklad_from" DATE;
