-- Архив партнёра: организация прекратила работу с нами. Пока archived_at задан —
-- счета не выставляются (крон generate-invoices), автоблокировка 1-го числа не
-- применяется (крон block-overdue), оплаты по выписке не матчатся (крон
-- check-payments), а сама организация исключается из MRR и статистики бэк-офиса.
-- В списке /admin/partners архивные уходят вниз с пометкой «Архив».
-- Обратимо: NULL → снова действующий партнёр.
ALTER TABLE "organizations" ADD COLUMN "archived_at" TIMESTAMP(3);
