-- Системные смены статуса клиента (кроны, вебхук ЮKassa, автоактивация абонемента)
-- не имеют сотрудника-инициатора. Разрешаем NULL в employee_id, чтобы такие
-- события попадали в историю карточки с автором «Система».
ALTER TABLE "audit_logs" ALTER COLUMN "employee_id" DROP NOT NULL;
