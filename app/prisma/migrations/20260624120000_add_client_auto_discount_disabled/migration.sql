-- Баг #50: явный запрет автоматических скидок для конкретного родителя
-- («Без скидки вручную»). true — тип 1 (автоскидка за второй абонемент) и
-- тип 2 (шаблон скидки) не действуют; уже выданные снимаются с оставшихся
-- занятий. Разовые скидки-бонусы на баланс остаются доступны.
-- Взаимоисключающе с discount_template_id.

ALTER TABLE "clients" ADD COLUMN "auto_discount_disabled" BOOLEAN NOT NULL DEFAULT false;
