-- Пер-организационное отключение системных видов посещений (баг #82).
-- Владелец может скрыть системный тип («Уваж. пропуск» / «Прогул») у себя, не
-- затрагивая другие организации. Значение живёт в пер-орг. оверрайде рядом с
-- «Разрешить отчисление»; эффективный isActive типа = НЕ is_disabled.
ALTER TABLE "attendance_type_withdrawal_overrides"
  ADD COLUMN "is_disabled" BOOLEAN NOT NULL DEFAULT false;
