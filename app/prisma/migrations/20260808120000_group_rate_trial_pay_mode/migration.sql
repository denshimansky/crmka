-- Режим оплаты пробного занятия для ставки группы.
-- NULL = наследовать личную ставку инструктора (текущее поведение для
-- существующих групп, без регрессии в расчёте ЗП); 'none'|'paid_only'|'all'
-- = перекрыть личную настройку для пробных этой группы (как ставка группы
-- перекрывает остальные условия). Аддитивно, backfill не требуется.
ALTER TABLE "group_salary_rates" ADD COLUMN "trial_pay_mode" TEXT;
