# Проверка расширения на живых мессенджерах

Это единственное, что нельзя проверить автоматически: нужен реальный вход в
мессенджер и переписка с родителем. Всё остальное (загрузка расширения, панель во
всех состояниях, сквозной путь до API, дедупликация сообщений) уже прогнано —
см. `docs/messenger-extension.md` §8.

Основная часть чек-листа — Telegram Web. Проверка канала **MAX** (Фаза 4) —
отдельным разделом в конце; там же условие, при котором MAX вообще можно
включать.

Занимает ~10 минут. Идти по порядку: каждый шаг проверяет свой кусок.

## Какой адрес CRM указывать

`https://msk1.umnayacrm.ru` — там боевая база (Timeweb), и это **единственный
адрес CRM, разрешённый расширению**: с 01.09.2026 в `host_permissions` остался
только он. `app.umnayacrm.ru` и `dev.umnayacrm.ru` из манифеста убраны —
вписать их в панели больше нельзя, запрос не пройдёт вовсе.

⚠️ Когда прод переключат на `app.umnayacrm.ru`, одной сменой адреса в панели не
обойтись: нужна новая версия в сторе с добавленным доменом. Chrome на добавление
host-права **отключает расширение до повторного согласия пользователя**, так что
переключение DNS придётся планировать вместе с выкаткой расширения.

Токен и адрес связаны: токен живёт в базе той CRM, где выпущен.

## Подготовка

1. **Выпустить токен.** Открыть CRM → `https://msk1.umnayacrm.ru/settings/extension`
   (страница скрыта, пункта меню нет — только по прямой ссылке) → «Выпустить
   токен» → назвать («рабочий ноут») → **скопировать сразу**: показывается один раз.
2. **Загрузить расширение.** `chrome://extensions` → включить «Режим
   разработчика» (тумблер справа сверху) → «Загрузить распакованное расширение»
   → выбрать папку `c:\dev\CRMKA\extension`.
   *Ожидаем:* карточка «Умная CRM — панель в мессенджерах», без красной плашки
   с ошибками.
3. **Открыть Telegram.** `https://web.telegram.org`, войти в аккаунт, с которого
   ведётся переписка с родителями.

> **После каждого `git pull` с изменениями в `extension/`:** `chrome://extensions`
> → кнопка ⟳ на карточке расширения. Браузер держит копию кода с момента
> загрузки и сам её не обновляет. Вкладку Telegram при этом обновлять не нужно —
> расширение само доинжектирует свой скрипт в открытые вкладки.

## Проверка

| # | Действие | Ожидаемый результат |
|---|---|---|
| 1 | Нажать иконку расширения на панели браузера | Справа открывается панель с экраном подключения |
| 2 | Вписать адрес `https://msk1.umnayacrm.ru` (он подставлен по умолчанию), вставить токен, включить «Записывать переписку в историю клиента», «Сохранить» | Экран подключения исчезает; если чат не открыт — «Откройте чат в Telegram» |
| 3 | Открыть любой чат с родителем | Панель пишет, что чат не связан с клиентом, и предлагает поиск |
| 4 | Ввести в поиск фамилию родителя | Появляются подходящие клиенты со статусом («Активный», «Лид») |
| 5 | Дописать через пробел имя («Малафеев Дима») | Клиент **остаётся** в списке: поиск ищет по словам, а фамилия и имя лежат в разных полях |
| 6 | Нажать на нужного | Появляется карточка: подопечные с возрастом, «Было»/«Будет», абонементы с остатком и долгом, баланс, платежи, переписка (последние 10 записей по всем каналам) |
| 7 | Сверить данные с карточкой в CRM (ссылка — по имени клиента в панели) | Совпадают: те же занятия, те же суммы |
| 8 | Переключиться на другой чат и обратно | Панель перерисовывается; в уже привязанный чат клиент подхватывается **сразу**, без поиска |
| 9 | Открыть в CRM карточку этого клиента → вкладка «Коммуникации» | Появились сообщения из чата с правильным направлением и каналом «Телеграм». **Только последние 10** — старая переписка целиком не заливается. В тексте **нет прилипшего времени** («дальше10:14»), а в шапке строки стоит время отправки сообщения, а не время заливки |
| 10 | Написать родителю новое сообщение (или попросить написать вам), **страницу не перезагружать** | Через 3–5 секунд сообщение само появляется в блоке «Переписка и события» в панели |
| 11 | В карточке нажать «Расписание» в блоке «Вставить в чат» | Текст с ближайшими занятиями появляется **в поле ввода Telegram**, ничего не отправляется; в панели — «Текст вставлен в поле ввода» |
| 12 | Проверить вставленный текст | Даты, время, направление и кабинет совпадают с расписанием в CRM; строки не слиплись в одну |
| 13 | Набрать в поле ввода своё начало фразы и нажать «Абонемент» | Справка дописывается к набранному, а не затирает его |
| 13а | Нажать «✨ Черновик ответа» | Кнопка пишет «Готовим…», через несколько секунд в поле ввода появляется вежливый ответ по последнему сообщению родителя — ничего не отправляется |
| 13б | Прочитать черновик | Он опирается на данные карточки и не обещает того, чего в них нет: переноса занятия, возврата, скидки |
| 13в | «Добавить в CRM» → «Задача», написать текст, срок: «Завтра» либо «Выбрать дату» (откроется календарь; выбранная дата встанет прямо на кнопку), «Сохранить» | «Задача создана»; в CRM «Задачи» она на вас, с этим клиентом и на выбранную дату |
| 13г | «Добавить в CRM» → «Комментарий», написать текст, «Сохранить» | «Комментарий записан»; он сразу появляется в блоке «Переписка и события» панели и в карточке клиента на вкладке «Коммуникации» как заметка |
| 14 | Нажать в шапке панели ⟳ | Значок крутится; карточка перечитывается целиком (данные, изменённые в CRM, подтягиваются) |
| 15 | Оставить панель открытой, в CRM провести оплату этому клиенту | В течение минуты баланс/платежи в панели обновятся сами (фоновая перепроверка раз в 60 секунд) |
| 16 | Не трогать браузер минуту (фоновый скрипт засыпает), затем открыть панель | Карточка на месте: расширение восстанавливает открытый чат само, перезагрузка Telegram не нужна |
| 17 | В Telegram переоткрыть тот же чат 2–3 раза, обновить вкладку «Коммуникации» | **Дублей нет** — сервер отбрасывает уже известные сообщения |
| 18 | В панели нажать «отвязать» | Возвращается экран привязки |
| 19 | В CRM `/settings/extension` нажать «Отозвать» у токена, в панели нажать ⟳ | Панель показывает ошибку авторизации — доступ пропадает сразу |

Отдельно стоит открыть Telegram в **обоих клиентах** — `web.telegram.org/k/` и
`web.telegram.org/a/` (переключаются прямой ссылкой): у них разный DOM, и
адаптера два. Проверить хотя бы шаги 3–6, 10 и 11 в каждом: поле ввода и пузыри сообщений у них разные.

## После стабилизации (правки 28.08.2026)

Эти пункты появились вместе с починкой найденного аудитом. Все они про пути,
которые раньше молча ломались, — глазами их не видно, пока специально не полезть.

| # | Действие | Ожидаемый результат |
|---|---|---|
| 20 | Открыть форму «Задача», набрать текст, **не сохраняя** переключиться на соседнюю вкладку браузера и вернуться | Набранный текст на месте. Раньше любое переключение вкладки перерисовывало панель и стирало форму |
| 21 | В той же вкладке уйти с Telegram на любой другой сайт | Карточка клиента пропадает, панель пишет «Панель работает поверх веб-мессенджера». Раньше карточка висела поверх постороннего сайта |
| 22 | Настройки → снять «Записывать переписку» → «Сохранить» → нажать «✨ Черновик ответа» | Панель пишет «Черновик читает переписку — включите её запись в настройках». Запрос **не уходит**: текст чата не покидает браузер |
| 23 | Открыть второе окно браузера с Telegram, в каждом окне своя панель и свой чат | Каждая панель показывает клиента своего окна; вставка попадает в чат своего окна |
| 24 | `chrome://extensions` → ⟳ на карточке расширения, вкладку Telegram **не** перезагружать, открыть её консоль (F12) | Нет ошибок «Extension context invalidated». Панель после ⟳ в своей шапке снова видит чат |
| 25 | Нажать «✨ Черновик ответа» и, пока идёт «Готовим…», переключиться на другой чат | Текст **не вставляется** в чужой диалог; статус «Чат сменился — текст скопирован, вставьте в нужный диалог» |
| 26 | Быстро переключиться между двумя привязанными чатами туда-обратно, затем открыть «Коммуникации» обоих клиентов в CRM | Сообщения не перемешались: у каждого клиента только его переписка |
| 27 | Открыть чат в клиенте `/a` (`web.telegram.org/a/`), дать переписке залиться, посмотреть «Коммуникации» в CRM | Сообщения идут **в правильном порядке** (у `/a` времени в разметке нет вовсе — сервер ставит время заливки со сдвигом по позиции). Раньше они уходили без времени и проваливались в конец ленты |
| 28 | Нажать «Расписание», затем сразу ещё раз «Расписание» | Текст дописывается один раз за нажатие — не задваивается внутри одного нажатия |
| 29 | Если у организации валюта не рубль (Настройки → Организация) | Суммы в панели показаны её символом, а не «₽» |
| 30 | Начать печатать в поиске клиента длинный запрос, не делая пауз | Итоговая выдача соответствует последнему набранному тексту, а не промежуточному |

## Каноникализация chatId (`/k` ↔ `/a`) — проверять обязательно

Раньше один и тот же собеседник в клиентах `/k` и `/a` давал разные
идентификаторы чата (`@username` против числа): привязка из `/k` в `/a` не
находилась, а переписка, залитая из обоих, задваивалась в карточке.

Теперь расширение берёт числовой peer id из видимой разметки `/k` (у личного
чата это число совпадает с тем, что `/a` пишет в адресную строку) и шлёт на
сервер ВСЕ увиденные идентификаторы; канон выбирает сервер.

**Пункт 2 — несущий. Пока он не сошёлся, канон не выкатывать:** вся конструкция
стоит на равенстве двух чисел, а проверить его можно только на живом аккаунте.

| № | Что делаем | Что ожидаем |
|---|---|---|
| 31 | Открыть личный чат в `/k`, зайти в ⚙ настройки панели | Внизу строка «Чат: … · канон: …» с источником (`composer+bubble` и т.п.). Если написано «в разметке нет числового peer id» — селекторы Telegram уехали, см. ниже |
| 32 | **Тот же собеседник:** в `/k` посмотреть число в строке диагностики, затем открыть его же в `/a` и посмотреть число в адресной строке | Числа обязаны **совпасть до цифры**. Расхождение = остановиться, канон не выкатывать |
| 33 | Открыть в `/k` чат с ником и чат без ника | В первом адрес показывает `#@ник`, во втором число; панель находит клиента в обоих, диагностика показывает канон |
| 34 | Чат, привязанный СТАРОЙ версией расширения по `@username`: открыть в `/k` новой версией, затем открыть тот же чат в `/a` | В `/k` привязка находится сразу; в `/a` карточка находится **сама**, ручная привязка не предлагается. Это и есть починка первой половины дефекта |
| 35 | Написать в чат одно новое сообщение, дать залиться из `/k`, затем открыть тот же чат в `/a` и дать залить снова | В карточке клиента **одна** строка, не две |
| 36 | То же в обратном порядке: сперва залить из `/a` (там времени в разметке нет вовсе), потом зайти из `/k` | У строки встаёт **настоящее** время сообщения, а не время заливки |
| 37 | В `/k` отправить сообщение и в момент отправки нажать ⟳ | В карточке не появляется строка с дробным id вида `…222237.0001` (сообщение ещё не отправлено) |
| 38 | В `/k` быстро попереключать 5–6 диалогов подряд кликами по списку, с включённой записью | Ни одной заливки в чужую карточку. В диагностике на переходных кадрах может быть отказ канона — это норма |
| 39 | В `/k` сделать shift+click по диалогу в списке (откроется попап предпросмотра) | Панель **не** переключается на этот чат и не заливает его переписку |
| 40 | Открыть групповой чат в `/k` и в `/a` | Канон не строится (диагностика: «групповой чат»), панель ведёт себя как раньше. Склеивать `-123` и `-100123` мы сознательно не пытаемся |
| 41 | Привязать чат к заведомо неверному клиенту, дать залить пару сообщений, затем перепривязать к верному | Переписка **переезжает** в правильную карточку. Раньше это было невозможно в принципе |
| 42 | Отвязать чат в `/a`, затем открыть его в `/k` | Он отвязан и там: снимается вся группа идентификаторов |
| 43 | В DevTools руками снять `data-peer-id` с поля ввода и пузырей активного чата, обновить панель ⟳ | Панель работает **как раньше**: находит клиента по привязке/телефону/хендлу, в диагностике показывает причину отказа канона. Ничего не падает и не пустеет |
| 44 | Проверить «Расписание»/«Черновик ответа» — вставку текста в поле ввода `/k` | Текст попадает в настоящее поле ввода, кнопка отправки Telegram активируется |

**Если канон не строится (пункт 31).** Это не авария: расширение возвращается к
прежнему поведению. Но починить надо — иначе дубли вернутся. В консоли страницы
`/k` проверить, что находится хоть один из источников:

```js
const c = document.querySelector("#column-center .chats-container > .chat.active:not(.chat-preview)")
c.querySelector(".input-message-input[data-peer-id]")?.dataset.peerId
[...c.querySelectorAll(".bubble[data-mid][data-peer-id]")].pop()?.dataset.peerId
```

Все непустые значения обязаны быть **одинаковыми**. Чинится в
`readNumericPeerId` (`src/content/telegram.js`); правило приёма числа — в
`src/common/telegram-peer.js`, оно под тестами.

## MAX (`web.max.ru`) — Фаза 4, только на СВОИХ аккаунтах

**Условие выката (спека §8):** канал MAX работает сначала минимум неделю на наших
аккаунтах, и всё это время смотрим, не прилетает ли предупреждение или разлогин.
В MAX один номер = один аккаунт, практика блокировок по номеру есть. Партнёрам
MAX не включаем, пока обкатка не пройдена.

Разметка MAX опор почти не даёт: **id сообщения в ней нет вовсе**, машинного
времени тоже, классы Svelte хешируются. Поэтому ключ дедупа синтезируется из
чата, направления, времени и текста, а время собирается из двух половин — часы из
пузыря, дата из капсулы-разделителя. Отсюда и главные пункты проверки: **дубли**
(пункт 47) и **время** (пункт 46).

Перед прогоном полезно (30 секунд, ловит регрессы нашей логики, но не изменения
разметки MAX): `cd app && node ../extension/tools/max-adapter-check.mjs`.

| № | Что делаем | Что ожидаем |
|---|---|---|
| 45 | Открыть `https://web.max.ru`, войти, открыть личный диалог с родителем; в панели ⚙ посмотреть строку диагностики | Адрес вида `web.max.ru/437719203`; панель предлагает найти клиента, в диагностике «Чат: 437719203 · канон: адрес» |
| 46 | Привязать чат к клиенту, включив «Записывать переписку» | В карточке появляется последняя переписка. **Время у строк — настоящее** (совпадает с часами на экране MAX), а не время заливки |
| 47 | Нажать ⟳ три раза подряд, затем уйти в другой чат и вернуться | В карточке **ни одного дубля**. Это несущий пункт: ключ синтетический, и если он «плавает», дубли не убрать штатно |
| 48 | Написать в чат новое сообщение (с телефона или второго аккаунта) | Панель обновляется сама за 3–5 секунд, сообщение появляется в блоке «Переписка и события» |
| 49 | Сделать в этом чате звонок (или найти в истории «Пропущенный вызов») | Строка звонка **не** попадает в карточку клиента как сообщение |
| 50 | Открыть чат, где есть фото/стикеры без подписи и фото С подписью | Пустые пузыри не заливаются; у фото с подписью в карточку уезжает подпись — **без** хвоста вида «Скачать • 143.85 KB» (если хвост попал — записать и чинить `JUNK` в `src/content/max.js`) |
| 51 | Проверить сообщения за прошлые дни (капсулы «Вчера», «2 июля 2026») | Даты в карточке совпадают с капсулами MAX, год правильный |
| 52 | Отправить самому себе многострочное сообщение из панели: «Расписание» → отправить руками | В поле ввода переносы строк сохранены; **в отправленном сообщении между строками нет лишней пустой строки** (единственный открытый вопрос Шага 1) |
| 53 | Проверить многострочное входящее сообщение (родитель написал в две строки) | В карточке строки не склеились в одну (склейка — чинится в `readCleanText`, `src/content/adapter-core.js`) |
| 54 | Открыть **групповой** чат (адрес с минусом, `web.max.ru/-78377804395205`) | Панель пишет «Это групповой чат…», привязку не предлагает. В CRM по этому чату **не появилось ни одной коммуникации** |
| 55 | Быстро попереключать 5–6 диалогов подряд, с включённой записью | Ни одного сообщения в чужой карточке. Пропущенная заливка — не беда: догоняется по ⟳ |
| 56 | Открыть Telegram в соседней вкладке и попереключаться между вкладками | Панель показывает чат той вкладки, что активна; переписка не смешивается |

### Если MAX сменил разметку — починка без публикации в стор

Свёрстанное у MAX меняется без предупреждения, а публикация новой версии
расширения — это дни ревью Chrome Web Store, всё это время канал мёртв у всех
сотрудников сразу. Поэтому селекторы можно переопределить с сервера
(`GET /api/ext/selectors`, MV3 запрещает удалённый код, но разрешает данные).

1. Найти новый селектор: открыть MAX, F12 → Console → вставить
   `extension/tools/max-probe.js` (он только читает разметку).
2. В `app/src/lib/ext/selector-config.ts` дописать переопределение и поднять
   `VERSION`, например:
   `const CHANNEL_OVERRIDES = { max: { bubble: '[class*="msgWrapper"]' } }`.
   Имена ключей — из объекта `DEFAULTS` в `extension/src/content/max.js`.
3. Запушить. Расширение подхватит конфиг само: кэш живёт 6 часов и обновляется
   при открытии панели; чтобы не ждать — в панели ⚙ «Сохранить» (это форсирует
   перечитывание) либо перезагрузить расширение.
4. Проверить, что переопределение принято: в панели ⚙, вторая строка внизу —
   поле `селекторы` должно показать `конфиг vN: применено [bubble]`.
   Написано `ОТКЛОНЕНО` — значение не того типа или селектор не разбирается;
   встроенное при этом осталось на месте, канал не сломан сильнее.
5. Когда починка доедет до самого расширения — **убрать переопределение**, иначе
   оно останется навсегда и однажды перебьёт правильный встроенный селектор.

**Счётчики пропусков — прямо в панели.** Открыть ⚙ настройки панели: внизу, под
строкой «Чат: …», вторая строка со счётчиками сбора и действующими селекторами.
Смотреть туда стоит раз в день всю неделю обкатки: поломка разметки молчалива —
карточка продолжает рисоваться, а переписка тихо перестаёт заливаться.

То же самое из консоли service worker (если панель недоступна):
`chrome://extensions` → у расширения ссылка «service worker» → в его консоли:

```js
chrome.tabs.query({active: true, currentWindow: true})
  .then(([t]) => chrome.tabs.sendMessage(t.id, {type: "ping"}))
  .then(console.log)
```

В поле `сбор` — `всего / взято / служебных / безНаправления / безВремени /
пустых`. Ненулевой `безНаправления` означает, что MAX переименовал
`data-bubbles-variant` или модификатор `--isOut`: тогда часть переписки молча не
заливается (это лучше, чем перепутанные направления, но чинить надо —
`readDirection` в `src/content/max.js`). Ненулевой `безВремени` — сломался разбор
капсул (`src/common/max-time.js`, под тестами). Рядом поле `селекторы`: там либо
«встроенные селекторы», либо что именно приехало из удалённого конфига.

## WhatsApp (`web.whatsapp.com`) — Фаза 5

**В России web.whatsapp.com заблокирован с 12.02.2026 — нужен VPN.** Это же
относится и к сотрудникам центров: канал имеет смысл включать только тем, кто
и так работает с WhatsApp через VPN.

**Сначала probe, потом всё остальное.** `extension/tools/wa-probe.js` — читает
разметку и ничего не меняет. Вставить в консоль на открытом личном чате,
повторить `crmkaWaProbe.rerun()` в группе, в «Сообщениях себе» и на списке чатов.
Главный вопрос прогона: JID в `data-id` у ваших контактов — это ещё номер
(`…@c.us`) или уже скрытый идентификатор (`…@lid`). От ответа зависит, находится
ли клиент автоматически по телефону или только ручной привязкой.

Перед прогоном полезно (30 секунд, ловит регрессы нашей логики, но не изменения
разметки WhatsApp): `cd app && node ../extension/tools/wa-adapter-check.mjs`.

Чем WhatsApp проще MAX: **у сообщения есть настоящий идентификатор**, поэтому
ключ дедупа не синтетический — правка сообщения не даёт вторую строку, а два
одинаковых сообщения в минуту не схлопываются. Чем сложнее: **адрес не меняется
при смене чата вовсе**, и чат узнаётся только по разметке; в пустом диалоге его
узнать неоткуда.

| № | Что делаем | Что ожидаем |
|---|---|---|
| 57 | Открыть `https://web.whatsapp.com`, войти, открыть личный диалог; в панели ⚙ посмотреть строку диагностики | Панель предлагает найти клиента. В диагностике — `Чат: <номер>@c.us` либо `<число>@lid`, и `канон: номер из JID` / `скрытый идентификатор…` |
| 58 | Если в диагностике `@c.us` и телефон клиента есть в CRM | Клиент найден **сам**, без ручной привязки — единственный канал, где так бывает |
| 59 | Если в диагностике `@lid` | Клиента выбираем руками. Проверить, что после привязки чат узнаётся при следующем заходе |
| 60 | Привязать чат к клиенту, включить «Записывать переписку» | В карточке появляется последняя переписка, **время настоящее** и **дата правильная** (12.08 — это август, а не 8 декабря) |
| 61 | Нажать ⟳ три раза, уйти в другой чат и вернуться | Ни одного дубля. Здесь это должно работать «само»: ключ — настоящий id сообщения |
| 62 | Отредактировать своё отправленное сообщение в WhatsApp, нажать ⟳ | Вторая строка в карточке **не** появляется (в MAX появилась бы — там ключ синтетический) |
| 63 | Быстро попереключать 5–6 диалогов подряд с включённой записью | Ни одного сообщения в чужой карточке. Здесь защита точная: каждое сообщение несёт свой чат |
| 64 | Открыть **групповой** чат | Панель пишет «Это не личный чат…», привязку не предлагает. В CRM по этому чату **ни одной коммуникации** |
| 65 | Открыть «Статусы» и, если есть, «Каналы» | То же самое: панель их не обслуживает |
| 66 | Открыть чат, в котором ещё **нет ни одного сообщения** | Панель пишет «Чат открыт, но в нём пока нет сообщений…» — отдельная причина, не «чат не выбран» |
| 67 | Прокрутить длинную историю вверх и обратно вниз | Панель не бегает в CRM на каждой прокрутке; в карточку не заливается старая история |
| 68 | Открыть чат с фото и голосовыми | Пустые пузыри не заливаются; у фото с подписью уезжает подпись без служебного хвоста |
| 69 | Найти в истории уведомление «Сообщения защищены сквозным шифрованием» и запись о звонке | В карточку **не** попали: у служебных строк нет классов направления, и отбраковка структурная |
| 70 | Вставить из панели многострочную справку и **отправить руками** | Переносы сохранены; в отправленном сообщении между строками нет лишней пустой строки |
| 71 | Убедиться, что панель ничего не отправила сама | В чате нет ни одного сообщения, которого вы не отправляли. Синтеза Enter в коде нет — но проверить глазами |

### Если WhatsApp сменил разметку

Порядок тот же, что для MAX (см. выше), только probe — `wa-probe.js`, а имена
ключей — из объекта `DEFAULTS` в `extension/src/content/whatsapp.js`:
`root`, `row`, `incoming`, `outgoing`, `text`, `prePlain`, `title`,
`composerField`, `junk`.

В счётчиках сбора у WhatsApp свои поля: `всего / взято / служебных / чужогоЧата /
безКлюча / безВремени / пустых`.

- ненулевой **`безКлюча`** — сломался разбор `data-id` (формат сменился);
- ненулевой **`чужогоЧата`** — норма при переключении диалогов: так выглядит
  разметка соседнего чата, которую мы правильно НЕ взяли;
- ненулевой **`служебных`** — норма: уведомления и звонки;
- растущий **`пустых`** при видимом тексте на экране — сломался селектор `text`;
- ненулевой **`безВремени`** — сломался разбор `data-pre-plain-text`. В отличие
  от MAX это НЕ мешает заливке: сообщение уедет в CRM со временем заливки.

## ВКонтакте — Фаза 6, ШАГ 1: probe разметки (ВЫПОЛНЕН 01.09.2026)

Разметка снята, адаптер написан — эти пункты оставлены как ИНСТРУМЕНТ на случай,
когда ВК сменит вёрстку. Для проверки самой панели см. следующий раздел. Пункты
ниже выполняет заказчик, отчёты присылает; по ним пишется адаптер.

**Где именно.** Сообщения СООБЩЕСТВА: `vk.com/gim<id сообщества>` (в управлении
сообществом — «Сообщения»). Это рабочая поверхность центров; личные сообщения
сотрудника (`vk.com/im`) проверяем вторым заходом, для сравнения.

Инструмент — `extension/tools/vk-probe.js`: только читает видимый DOM, ничего не
отправляет и не трогает сессию. F12 → Console → напечатать «allow pasting» →
вставить весь файл → Enter. Отчёт ляжет в буфер обмена.

| № | Что сделать | Что прислать / ожидаемое |
|---|---|---|
| 72 | Открыть диалог с родителем в сообщениях сообщества, прогнать probe | Отчёт целиком. `адрес.ключЧата` = число после `convo`, `адрес.источникId` = «путь /convo/<id>» |
| 72а | Посмотреть `ссылкиНаПрофили.совпадениеСПрофилем` | **Открытый вопрос:** `true` значит в `/convo/<N>` лежит id пользователя, и поле «ВКонтакте» в карточке заработает автоподсказкой; `false` — номер внутренний, привязка только ручная |
| 72б | Посмотреть `сообщения.строки[].идентификаторы` | **Открытый вопрос:** есть ли у сообщения свой id. Кандидат — `предок-1:data-itemkey`. Если это id, ключ дедупа будет настоящий, а не синтетический, как в MAX |
| 73 | Не перезагружая страницу, открыть ДРУГОЙ диалог того же сообщества, `crmkaVkProbe.rerun()` | **Главная проверка.** `ключЧата` обязан ОТЛИЧАТЬСЯ от пункта 72, а адрес в строке браузера — смениться. Если ключ тот же — вся переписка сообщества склеится в одну карточку, это стоп-сигнал |
| 73а | Там же: прокрутить историю ВВЕРХ и снова `rerun()` | Сдвинулись ли значения `data-itemkey` у тех же сообщений. Сдвинулись — это индекс виртуального списка (ключ придётся синтезировать); остались — настоящий id сообщения |
| 74 | Посмотреть в отчёте раздел `якорь` | Где ВК держит id собеседника в разметке. Пусто — значит чат опознаётся только по адресу |
| 75 | Посмотреть `сообщения.строки` | Есть ли у строки СВОЙ идентификатор и машинное время; чем отличается исходящее от входящего |
| 76 | Открыть личные сообщения (`vk.com/im`), выбрать диалог, `rerun()` | Форма адреса в личке: тот же ли `sel` |
| 77 | Открыть БЕСЕДУ (если есть), `rerun()` | `адрес.вид` = «беседа», ключ вида `c<N>`. Такие чаты панель вести не будет |
| 78 | Вернуться в список диалогов, не открывая ни одного, `rerun()` | `вид` = «мессенджер открыт, диалог не выбран», `ключЧата` = null |
| 79 | `await crmkaVkProbe.tryInsert("exec")`, затем `("paste")` | Какой способ принимает поле ввода и сохраняет ли переносы строк. **Текст останется в поле — стереть вручную**, probe ничего не отправляет |
| 80 | `crmkaVkProbe.watch()`, попросить написать в диалог, затем `stop()` | Ловится ли новое сообщение наблюдателем DOM (от этого зависит обновление панели на лету) |
| 81 | `await crmkaVkProbe.scrollCheck()` | Виртуализируется ли лента (для нас не страшно — берём хвост, но знать надо) |

Если ВК откроется в отдельном интерфейсе (`web.vk.me`) — прогнать probe и там
тоже: разметка у него своя, и адаптеру придётся различать оба.

## ВКонтакте — Фаза 6, живой прогон ПАНЕЛИ

Разметка снята четырьмя прогонами probe 01.09.2026, адаптер написан по ней и
проверен в Chromium (`tools/vk-adapter-check.mjs`, 27 проверок). Осталось
прогнать саму панель на живых чатах.

**Где.** Сообщения СООБЩЕСТВА: `vk.ru/gim<id>` → «Сообщения». Личка
администратора (`vk.ru/im`) тоже поддержана, но рабочая поверхность — первая.

| № | Что сделать | Ожидаемое |
|---|---|---|
| 82 | Открыть диалог с родителем | Панель находит клиента либо предлагает привязку; в диагностике `kind: chat`, путь `/gim…/convo/<id>` |
| 83 | Привязать чат к клиенту, перезайти в диалог | Клиент опознан сразу, привязка не «слетает» |
| 84 | Вписать в карточку клиента ссылку `vk.ru/id<id>` или короткое имя, открыть его диалог | Панель предлагает этого клиента САМА — ради этого адаптер и шлёт второй идентификатор |
| 85 | Переключиться на ДРУГОЙ диалог того же сообщества | Карточка сменилась целиком; переписка первого клиента во второго не попала |
| 86 | Включить запись переписки, обновить панель (⟳) | В карточке появились последние сообщения — своими датами, а не датой заливки |
| 87 | Нажать ⟳ три раза подряд | Дублей в карточке НЕТ: ключ дедупа детерминирован |
| 88 | Проверить направление в карточке | Сообщения родителя — входящие, ответы центра — исходящие. Особенно там, где подряд идёт несколько сообщений одного автора |
| 89 | Найти сообщение центра, на которое родитель поставил реакцию | Оно осталось ИСХОДЯЩИМ. Класс реакции `--incoming` — ловушка, из-за неё наша реплика могла бы уехать в карточку как слова клиента |
| 90 | Открыть беседу или диалог с сообществом | Панель отказывается и объясняет: «Это беседа или диалог с сообществом». Ни резолва, ни заливки |
| 91 | Открыть список диалогов, не выбирая чат | «Откройте чат», без ошибок |
| 92 | Вставить справку или ИИ-черновик в поле ввода | Текст вставлен, переносы строк сохранены, НИЧЕГО не отправлено |
| 93 | Попросить написать в открытый диалог | Панель обновилась на лету, новое сообщение появилось в карточке |
| 94 | Прокрутить историю далеко вверх и обратно | В карточку не залилась старая переписка целиком: берётся только хвост ленты |
| 95 | Посмотреть счётчики сбора в настройках панели | `всего` > 0, `взято` > 0. Ненулевой `безВремени` — сломался разбор разделителя дня; `безНаправления` — разбор маски аватара |

### Если ВКонтакте сменил разметку

Порядок тот же, что для MAX и WhatsApp, только probe — `vk-probe.js`, а имена
ключей — из объекта `DEFAULTS` в `extension/src/content/vk.js`: `feed`, `row`,
`separator`, `text`, `clock`, `status`, `avatar`, `header`, `headerTitle`,
`headerLink`, `composerField`, `junk`.

Счётчики сбора у ВК: `всего / взято / безНаправления / безВремени / пустых`.

- ненулевой **`безНаправления`** — сломался разбор маски аватара или статуса
  доставки. Это ГЛАВНЫЙ счётчик канала: направление у ВК выводится, а не
  читается из класса, и его поломка тише прочих;
- ненулевой **`безВремени`** — не разобрался разделитель дня (`separator`).
  Заливка встанет целиком: без даты ключ сообщения недетерминирован;
- ненулевой **`пустых`** — норма: стикеры, фото и голосовые без подписи.
## Если что-то не работает

- **Панель пустая или не реагирует** → правый клик по панели → «Просмотреть код»
  → вкладка Console: ошибки панели видны там.
- **Панель не знает про открытый чат** («Откройте чат…», хотя чат открыт) →
  сломался адаптер Telegram. Консоль страницы Telegram (F12) → проверить, что
  content script загрузился. Скорее всего изменился формат хэша — чинится в
  `src/common/telegram-hash.js`, там же тесты.
- **Ошибки запросов к CRM** → `chrome://extensions` → у расширения ссылка
  «service worker» → откроется его консоль: там видны все обращения к API.
- **«Недействительный токен»** → токен отозван или скопирован не полностью.
- **Сообщения не попадают в CRM** → проверить, что в настройках панели включён
  тумблер «Записывать переписку»: по умолчанию он **выключен**.
- **Новое сообщение не появилось само** → нажать ⟳ в шапке панели. Если по
  кнопке появилось, а само нет — сломался наблюдатель за DOM в
  `src/content/telegram.js` (`reportActivity`), это чинится там же.
- **Справка не вставилась** («текст скопирован, вставьте вручную») → не нашлось
  поле ввода: чинить `findComposer` в `src/content/telegram.js`. Текст при этом
  не теряется — он в буфере обмена.
- **«Чат сменился — текст скопирован»** → это не поломка: расширение отказалось
  вставлять текст, потому что за время запроса открылся другой диалог. Текст в
  буфере обмена.
- **Переписка перестала заливаться после правок** → сузился сбор сообщений: берём
  контейнер активного чата, а не весь документ. Если контейнер найден, но пуст,
  мы сознательно ничего не заливаем (подмена документом утащила бы в карточку
  переписку соседнего чата). Чинить — `messagesRoot` в
  `src/content/telegram.js`: проверить в консоли страницы, что
  `document.querySelector(".chat.active")` (клиент `/k`) или
  `document.querySelector("#MiddleColumn")` (клиент `/a`) находит открытый диалог.

- **MAX: панель не видит открытый чат** → адрес не разобрался. В консоли страницы
  `location.pathname` обязан быть одним числовым сегментом (`/437719203`).
  Правило — в `src/common/max-path.js`, оно под тестами; многосегментный адрес мы
  сознательно чатом не считаем.
- **MAX: текст не вставился** → поле ввода там Lexical, и оно объявлено как
  `contenteditable=""` (пустое значение!). Проверить в консоли:
  `document.querySelector('[data-testid="composer"] [contenteditable]')`. Чинить —
  `findComposer` в `src/content/max.js`. Способ вставки менять на `execCommand`
  **нельзя**: он теряет переносы строк (проверено на живом MAX 31.08.2026).
- **MAX: переписка не заливается, хотя тумблер включён** → смотреть счётчики
  пропусков (рецепт выше). Пустой `взято` при ненулевом `всего` — сломался либо
  разбор направления, либо разбор времени; и то и другое чинится точечно.

## Что записать по итогам

Если найдутся расхождения — записать, в каком клиенте (`/k` или `/a`), на каком
шаге и что именно не сошлось. Селекторы DOM у Telegram меняются, и это ожидаемая
точка ремонта; всё остальное к мессенджеру не привязано.
