# Браузерное расширение-панель CRMka в мессенджерах (аналог BubbleXT)

> Статус: **в разработке, Фаза 1 (бэкенд)**. Обновлено 28.08.2026.
> Рабочая спека проекта — **единая точка правды** при разработке с любой машины
> (десктоп/ноут). План реализации одобрен 28.08.2026; аналитика проведена
> (разбор BubbleXT, аналогов, технический R&D по 4 мессенджерам, сторы, 152-ФЗ).

## 1. Что делаем

Первопартийное **MV3-расширение** для Chromium-браузеров (Chrome / Яндекс / Edge).
Сотрудник детского центра переписывается с родителем в веб-мессенджере — сбоку
открывается панель с карточкой клиента из CRMka, из неё же доступны действия.
Расширение дополнительно **пишет увиденные сообщения в единую кросс-канальную
историю коммуникаций** CRM: родитель писал в одном мессенджере, а сегодня пишет
в другом — администратор видит всю переписку в одном месте.

**Референс — BubbleXT** (bubblext.ru), надстройка над конкурентом AlfaCRM, та же
ниша (детские центры). Разбор показал: продукт запущен ~8 июля 2026, **17
пользователей, ноль отзывов**, делает семейное ИП. Ниша фактически свободна.
Наше преимущество: CRM своя → нормальная первопартийная авторизация по токену и
серверный агрегатор вместо их «локального» костыля с пользовательским API-ключом
AlfaCRM.

## 2. Приоритет каналов: Telegram → MAX → VK → WhatsApp

**Важно: прежняя стратегия «WhatsApp-first» отменена.** С 12.02.2026 домены
whatsapp.com и web.whatsapp.com исключены из национальной системы доменных имён —
в РФ WhatsApp доступен только через VPN; Telegram с 10.02.2026 замедляется.
Одновременно государство перевело родителей (нашу ЦА) в **MAX**: Сферум встроен
в MAX, школьные и садовские чаты переведены туда с учебного года 2025/2026.

| Канал | Идентификация собеседника | Сообщения | Статус |
|---|---|---|---|
| **Telegram** | URL-hash: `/k` → `#@username` или `#<id>`, `/a` → всегда числовой `#<id>`; страховка (WebK) — MAIN-world `window.appImManager.chat.peerId`. **Телефона нет** | DOM: WebK `.bubble[data-mid]` + `is-in`/`is-out`; WebA `#message-*` + класс `own` | **Фаза 2 — первый канал** |
| **MAX** | `web.max.ru/<числовой chatId>` — первый сегмент пути (SvelteKit, хэша нет). Телефон закрыт настройкой `PHONE_NUMBER_PRIVACY`, @username у людей нет вовсе → только ручная привязка | DOM: `.messageWrapper` + `--out`, текст в `.message`, время строкой в `.meta`. **Ни id сообщения, ни машинного времени в разметке нет** | **Фаза 4 — делаем** |
| **VK** | peer id прямо из URL: `vk.com/gim<группа>?sel=<user>` (главная поверхность — сообщения сообщества) | DOM хрупкий; **есть официальный API сообществ** | Фаза 6, путь решаем после ядра |
| **WhatsApp** | wa-js (WPP) → номер; но **LID (`@lid`) нарастает**, с 2026 — usernames/BSUID | Store: `getMessages` (`fromMe`, `t`, `MsgKey.id`, `body`) | Фаза 5 |

Почему Telegram первым: идентификация и DOM изучены лучше всех, оба клиента
(`/k` WebK на Solid, `/a` WebA на React) имеют открытые исходники — риск минимален,
результат быстрый. Ограничение честное: **нативный Telegram Desktop расширением не
покрывается** (для полноты позже — серверный путь через Bot API).

Для VK возможен гибрид: Callback API сообществ отдаёт **обе стороны** переписки
(событие `message_reply` срабатывает и когда админ отвечает руками из интерфейса
VK), плюс `messages.getHistory` ретроспективно и легальная отправка. Тогда
расширение в VK ничего не скрейпит, а только рисует панель по peer id из URL.
Решение — после продакшена ядра.

## 3. Архитектура

- **UI — нативный Chrome Side Panel API** (`sidePanel`), а не инъекция панели в
  страницу. Не зависит от вёрстки мессенджера (не ломается при их обновлениях),
  единый UI на все каналы, штатно работает в Chrome/Edge/Яндекс. Так же сделал
  BubbleXT.
- **Content script** на хостах мессенджеров — только детект активного чата и
  чтение видимых сообщений; при необходимости **MAIN-world** скрипт (`world:
  "MAIN"`) для доступа к внутренним структурам SPA, мост в isolated-мир через
  `postMessage`.
- **Service worker** — хранит PAT в `chrome.storage`, ходит в `/api/ext/*`
  (у него `host_permissions` → CORS контент-скрипта не мешает), рулит панелью.
- **Единый UI + сменные адаптеры на канал**. Интерфейс адаптера:
  `detectActiveChat() → {channel, chatId, handle?, phone?, title?}` и
  `readRecentMessages(n) → [{externalId, direction, text, sentAt}]`.
- **Remote JSON-конфиг** для селекторов/идентификаторов модулей/флагов: MV3
  запрещает remote **code**, но разрешает remote **данные** — это даёт починку
  сломавшегося канала без перепубликации в стор (ревью — дни).
- **Graceful degradation + телеметрия**: панель живёт, даже если чтение сообщений
  канала сломалось; событие «не нашли чат/сообщения» — ранний сигнал поломки.

### Принцип-щит (обязателен к соблюдению)

**Расширение читает только то, что администратор и так видит на экране, и ничего
не отправляет автоматически.** Никаких рассылок, автоответов, «отправить из CRM» —
шаблоны и справки вставляются в поле ввода, отправляет человек. Это выводит нас
из-под формулировок про spam/automation: из Chrome Web Store в октябре 2025
выпилили 131 расширение именно за массовые рассылки и клоны, а легитимные
CRM-сайдбары (Cooby) живы; баны аккаунтов у Meta/Telegram/VK тоже привязаны к
отправке.

## 4. Модель данных в CRMka

**Уже есть:** поля-хендлы `telegram` / `vk` / `max` в `Client` (миграция
`20260828120000_client_messenger_handles`) — свободный текст, проведены через
API create/edit/merge, формы, блок «Информация», PageHelp. Модель `Communication`
уже имеет `channel`, `direction`, индексированный `externalId`, `metadata`,
`employeeId`.

**Добавляем (Фаза 1):**
- `Communication.sentAt` — реальное время сообщения (сейчас `createdAt` = момент
  записи); ленты сортируем `COALESCE(sentAt, createdAt) desc`.
- `@@unique([tenantId, channel, externalId])` — ключ идемпотентности batch-заливки
  (панель видит одни и те же последние сообщения при каждом открытии чата).
- `CommunicationChannel` += `vk`, `max`; `CommunicationType` += `messenger_incoming`,
  `messenger_outgoing` (канал уже в `channel`, типы generic).
- **`ApiToken`** — персональный токен сотрудника (PAT): `tokenHash` (sha256) `@unique`,
  `prefix`, `scopes`, `lastUsedAt`, `expiresAt`, `revokedAt`. Секрет `crmka_` +
  `randomBytes(32).base64url`, показывается один раз.
- **`ChatBinding`** — привязка чат ↔ клиент: `channel`, `externalChatId`, `clientId`,
  `wardId?`, `createdBy`, `lastSeenAt`; `@@unique([tenantId, channel, externalChatId])`.
  **Это ядро идентификации**: матч по телефону надёжен только в WhatsApp, поэтому
  привязка ручная (с автоподсказкой) и запоминается.
- ~~`MessageTemplate`~~ — **отменено 28.08.2026** (см. §8): таблица заведена
  миграцией Фазы 1 и осталась пустой. Шаблоны ответов из состава вырезаны.

**Попутный фикс:** вебхук Wazzup пишет коммуникации без дедупа и кладёт в
`externalId` chatId (телефон), а не id сообщения; время сообщения теряется в
`metadata`. Чиним под новую модель отдельным коммитом.

## 5. API-поверхность `/api/ext/*`

Авторизация — `Authorization: Bearer <PAT>`; `/api/ext` добавляется в исключения
matcher `middleware.ts` (иначе next-auth редиректит на /login), поэтому режим
неплательщика и права энфорсим в самом гарде. Гард `lib/ext-auth.ts` — по образцу
`portal-auth`: читает БД на каждый запрос, поэтому отзыв токена, увольнение
сотрудника, смена филиалов и блокировка биллинга действуют мгновенно (без
5-минутного лага JWT). **Права токена = скоупы ∩ права роли по матрице ∩ филиалы
сотрудника.** CORS — строгий allow-list источников мессенджеров + `OPTIONS`.

| Эндпоинт | Назначение |
|---|---|
| `GET /api/ext/resolve` | чат → клиент: сперва `ChatBinding`, затем автоподсказка (WA — `findClientsByPhone`; прочие — по хендлам/имени) |
| `POST/GET/DELETE /api/ext/bindings` | ручная привязка чата к клиенту |
| `GET /api/ext/client-card` | агрегатор одним запросом (см. §6) |
| `POST /api/ext/communications/batch` | заливка увиденных сообщений, дедуп по unique-ключу |
| `GET /api/ext/clients/search` | поиск клиента для ручной привязки (по словам ФИО и по телефону) |
| `POST /api/ext/tasks`, `/comments` | задача и комментарий из чата (`logClientNote` — единая точка) |
| ~~`POST /api/ext/leads`~~ | лид из незнакомого чата — **отменено 28.08.2026**, см. §8 |
| ~~`GET /api/ext/templates`~~ | шаблоны ответов — **отменено 28.08.2026**, см. §8 |
| `GET /api/ext/quick-info` | «вставить справку»: расписание ребёнка / остаток по абонементу текстом |
| `POST /api/ext/ai-reply` | ИИ-черновик ответа (наш релей; не автоотправка) |

Токены выпускаются на скрытой странице **`/settings/extension`** (роут есть,
плитки и пункта меню нет — прецедент «Интеграции» с `show:false`).

## 6. Содержимое панели

- **Клиент:** имя, статус, филиал, телефон (с маскировкой по `instructorsSeePhones`),
  денежный баланс (`clientBalance`, минус = долг), ссылка на карточку в CRM.
- **Подопечные:** имя, возраст (`lib/age.ts`), **последнее прошедшее** и
  **ближайшее будущее** занятие.
- **Активные абонементы:** направление/группа, период, цена со скидкой, остаток
  занятий, долг (`max(0, Subscription.balance)`).
- **Последние платежи** (5) — без виртуальных `transfer_in`.
- **Последние 10 сообщений по всем каналам** — кросс-мессенджерная история
  (столько же панель и заливает за проход, `SYNC_MESSAGES_LIMIT`).
- **Действия MVP:** привязать чат / создать лида, задача, комментарий,
  «вставить справку», ИИ-черновик. (Шаблоны ответов были в плане и отменены —
  см. §8.)
- **v2:** платёжная ссылка из чата (ЮKassa/СБП), виджет «диалоги без ответа»,
  запись на пробное из панели.

Подводные камни (проверены по коду): пакетные абонементы (`periodYear = NULL`)
выпадают из наивного фильтра активности — брать критерий `getWardSubscriptions`
(`lib/portal-data.ts`); `GET /api/payments` требует право `finance.view` (роль без
него получит 403 — в агрегаторе считаем сами); исторические enrollments/attendance
с `wardId = NULL` требуют фоллбэка на клиентский уровень; возраст пуст у
импортированных клиентов (в файле не было даты рождения).

## 7. Дистрибуция и юридика

- **Chrome Web Store, режим unlisted** (не в каталоге, установка по прямой ссылке
  из онбординга CRMka). Одна публикация покрывает Chrome (47%) + Яндекс Браузер
  (32%) + Opera (7,5%) ≈ **87% десктопа РФ**. Edge Add-ons — отдельной публикацией
  позже (~день работы).
- **Safari — не портируем.** 1,77% десктопа РФ, только через App Store, Apple
  Developer $99/год + ревью. Дёшево подстраховаться: кросс-браузерный `browser.*`,
  фон как event page с `preferred_environment`, без `webRequest`.
- **Комплаенс CWS (с 01.08.2026):** узкий single purpose, prominent disclosure
  сбора переписки в листинге и в UI при первом запуске, отдельная privacy policy,
  собираем строго необходимое, ноль функций автоотправки.
- **152-ФЗ:** центр — оператор, CRMka (ИП Шиманский) — обработчик по поручению →
  приложение-поручение к оферте CRMka (одно на всех партнёров) + шаблон
  **отдельного согласия** для центров (с 01.09.2025 согласие — отдельный документ)
  с упоминанием фиксации переписки в мессенджерах и обработчика. Данные в РФ
  (Timeweb МСК) — 242-ФЗ соблюдён. Минимизация: последние N сообщений, без медиа
  (штрафы за утечки 3–15 млн, повторно — оборотные).
- **Безопасность:** никогда не читать localStorage/IndexedDB/сессионные токены
  мессенджеров (именно их воруют зловредные расширения) — только видимый DOM,
  URL-hash и публичные поля.

## 8. Фазы и статус

- [x] Поля-хендлы `telegram`/`vk`/`max` в карточке клиента (28.08.2026).
- [x] Глубокая аналитика + план (28.08.2026): порядок каналов, side panel,
      ChatBinding, PAT, состав MVP, дистрибуция, юридика.
- [x] **Фаза 1 — бэкенд (28.08.2026, задеплоено на прод):**
      - миграции `20260828140000_ext_messenger_backend` (sent_at, unique-ключ
        дедупа, каналы vk/max, типы messenger_*, таблицы chat_bindings /
        message_templates / api_tokens) и `20260828150000_communication_sent_at_default`
        (дефолт now() + бэкфилл: sent_at = «время события» у всех 12 548 строк);
      - `lib/ext-auth.ts` (PAT sha256, проверка из БД на каждый запрос),
        `lib/ext-cors.ts` (allow-list + preflight), `api/ext` в исключениях matcher;
      - `/api/api-tokens` + скрытая страница `/settings/extension` + PageHelp;
      - `/api/ext/{resolve,bindings,client-card,communications/batch}`,
        `lib/ext/{chat-identity,resolve-client,client-card}.ts`, 30 юнит-тестов;
      - фикс вебхука Wazzup: дедуп по id сообщения, `findClientsByPhone`, sent_at.
      **Проверено на боевых данных msk1:** резолв по номеру (в т.ч.
      форматированному), карточка (подопечный + возраст + прошедшее/ближайшее
      занятие + абонементы с долгом + платежи), привязка чата, идемпотентная
      заливка (повтор → `created:0, skipped:3`), кириллица в UTF-8, отзыв токена
      действует мгновенно, чужой origin не получает CORS. Тестовые данные удалены.
      `/api/ext/{tasks,comments}` доделаны в Фазе 3, `/leads` отменён заказчиком.
- [x] **Фаза 2 — расширение (28.08.2026):** `extension/` без сборщика
      (ES-модули, JSDoc + `checkJs`; npm только для типов и тестов). Service
      worker (настройки, чат по вкладкам, все запросы к API), боковая панель
      (подключение, карточка, выбор клиента, поиск, ручная привязка), адаптер
      Telegram Web с двумя DOM-наборами (WebK `/k` и WebA `/a`); разбор хэша
      вынесен в `common/telegram-hash.js` и покрыт 21 тестом. Добавлен
      `/api/ext/clients/search` (в Telegram телефона нет — клиента ищет человек).
      **Проверено:** расширение загружается в Chromium, service worker стартует,
      манифест принят; панель отрисована во всех состояниях (карточка, привязка
      с поиском, подключение, «откройте чат»); сквозной путь панель → service
      worker → API на боевом msk1 (поиск, резолв, карточка), неверный токен даёт
      понятную ошибку. Исправлена ошибка манифеста (`default_locale` без папки
      `_locales`), из-за которой Chrome отказался бы грузить расширение.
      **Осталось:** прогон на живом Telegram Web с авторизованным аккаунтом —
      единственное, что нельзя проверить автоматически. Пошаговый чек-лист:
      `extension/TESTING.md`.
      **По итогам первого живого прогона (28.08.2026) доработано:**
      • панель обновляется на лету — content script ловит новое сообщение
        наблюдателем DOM (своего события у Telegram нет) и через service worker
        будит панель; та заливает сообщение и тихо перечитывает карточку.
        Дребезг гасится тройным фильтром: дебаунс 300 мс в странице, сравнение
        «отпечатка» последнего сообщения, throttle 3 с в панели;
      • кнопка ⟳ в шапке панели — полное обновление вручную; плюс фоновая
        перепроверка карточки раз в 60 секунд (оплату проводят в CRM, из чата
        сигнала о ней нет);
      • чат восстанавливается после сна service worker: MV3 усыпляет фоновый
        скрипт через ~30 с, и карта «вкладка → чат» терялась — теперь панель
        доспрашивает content script (ответ на `ping` несёт весь контекст чата).
        Раньше это лечилось только перезагрузкой вкладки Telegram;
      • в CRM заливаются **последние 10** сообщений чата, а не вся подгруженная
        история (`SYNC_MESSAGES_LIMIT`): иначе первое открытие чата топило ленту
        коммуникаций старой перепиской. Столько же панель и показывает;
      • время сообщения отделено от текста. Telegram кладёт часы ВНУТРЬ блока
        текста (`<span class="time">` в конце пузыря), и наивный `textContent`
        склеивал их с сообщением («дальше10:14»); теперь текст читается по
        копии узла с вырезанными временем, цитатой и реакциями. Машинного
        времени в разметке WebK нет вовсе (на пузыре только `data-mid`) —
        поэтому дату берём из подсказки `title` у `.time-inner`, куда tweb
        пишет `getFullDate()`. Разбор — `common/telegram-time.js`, 15 тестов
        (месяц словом и числом, ru/en, «Edited»-строки, мусор → null). Без него
        вся переписка ложилась в CRM временем заливки: в ленте у всех строк
        стояло одно и то же время;
      • поиск клиента ищет **по словам** — «Малафеев Дима» находится так же, как
        «Малафеев» (имя и фамилия лежат в разных колонках, и уточнение запроса
        обнуляло выдачу). Заодно закрыта утечка: собственный `OR` поиска
        перетирал `OR` филиального scope, и находились клиенты чужих филиалов.
- [ ] **Фаза 3 — действия из панели (в работе, 28.08.2026):**
      - [x] **Вставка текста в поле ввода** — новая механика content script и
            фундамент для шаблонов и ИИ-черновика. Поле ищем по видимому
            contenteditable (у обоих клиентов их в разметке несколько: поиск,
            подпись к медиа, скрытые чаты). Вставляем через `execCommand`, а не
            правкой `textContent`: мессенджеры — SPA (Solid у WebK, React у
            WebA) и следят за полем через события ввода, прямая правка DOM их не
            будит — поле выглядит заполненным, но клиент считает его пустым и не
            активирует отправку. Переводы строк — отдельной командой
            `insertLineBreak` (в `insertText` браузеры обрабатывают «\n»
            по-разному, а справка многострочная). Отправку не инициируем нигде.
      - [x] **«Вставить справку»** — `GET /api/ext/quick-info` +
            `lib/ext/quick-info.ts`: расписание ближайших занятий (5), остаток
            по абонементу с долгом, баланс. Текст уходит РОДИТЕЛЮ, поэтому
            форматирование — чистые функции с 19 юнит-тестами (склонение
            месяца, день недели без сдвига зоны, валюта организации, имена детей
            только когда их несколько, пакетные абонементы без периода). Не
            нашли поле ввода — панель кладёт текст в буфер обмена.
      - [x] **ИИ-черновик ответа** (28.08.2026): `POST /api/ext/ai-reply` +
            кнопка «✨ Черновик ответа» в панели. Контекст модели — только
            карточка клиента (те же факты, что в справке) и последние 10
            сообщений, которые сотрудник видит на экране; ни чужих клиентов, ни
            финансов организации. Главный риск — выдуманные обещания («перенесём
            занятие», «вернём деньги», «скидка»), поэтому промпт их прямо
            запрещает и разрешает переспросить; тесты фиксируют эти запреты.
            Черновик уходит В ПОЛЕ ВВОДА, отправляет человек. Работа с
            провайдером вынесена в `lib/ai-provider.ts` (выбор провайдера,
            модели, релея и prompt cache) — общая точка с ИИ-ассистентом CRM,
            `/api/ai/chat` переведён на неё же: настройка релея хрупкая, двух
            копий быть не должно. Лимит общий — 50 обращений в сутки на
            организацию, журнал общий (`ai_chat_logs`).
      - [~] **Шаблоны ответов — ОТМЕНЕНЫ 28.08.2026, код вырезан.** Были
            сделаны и работали (страница в настройках, подстановка данных
            клиента на сервере, кнопки в панели), но по решению заказчика
            функционал не нужен: администратору хватает справки и ИИ-черновика,
            а ещё один справочник в настройках — лишняя сущность на поддержке.
            Удалено: `/settings/message-templates`, `/api/message-templates`,
            `/api/ext/templates`, `lib/ext/templates.ts`,
            `lib/ext/template-placeholders.ts`, ряд кнопок в панели, PageHelp.
            Осталась пустая таблица `message_templates` из миграции Фазы 1 —
            дропать отдельной миграцией, если решим. **Не делать заново без
            явного запроса.**
      - [x] **Задача и комментарий из чата** (28.08.2026): `POST /api/ext/tasks`
            и `POST /api/ext/comments` + форма в панели. Задача ставится на
            себя (выбор коллеги в узкой панели превратился бы в отдельный
            экран, а «поставить другому» есть в CRM) и БЕЗ филиала — ровно как
            ручная задача в CRM: расходиться с ней в видимости нельзя.
            Комментарий пишется через `logClientNote` — единую точку ленты
            коммуникаций, чтобы заметка из панели выглядела как заметка из CRM.
            **Грабли боковой панели:** родной пикер `input[type=date]` в ней не
            открывается вовсе — ни кликом по иконке, ни через `showPicker()`
            (ошибки при этом нет, просто ничего не происходит). Поэтому срок
            задачи ставится кнопками «Сегодня/Завтра» и своим календарём в
            разметке; `input` остался только держателем значения. Отсюда же
            правило: любой родной попап в панели надо проверять руками, а не
            считать, что он «просто работает».
      - [~] **Лид из незнакомого чата — отменён 28.08.2026** (решение
            заказчика): `POST /api/ext/leads` не делаем. Незнакомый чат
            по-прежнему привязывается к существующему клиенту через поиск.
- [x] **Фаза 3.1 — стабилизация по итогам аудита (28.08.2026).** Перед вторым
      каналом прогнали сквозной аудит бэкенда, кода расширения, долгов и
      готовности к публикации (29 агентов, находки перепроверены адверсарно;
      две отклонены — в том числе «guard инжекции мешает самовосстановлению»,
      опровергнутая экспериментом в Chromium). Починено:
      - **Блокер: заливка писала `sent_at = NULL`.** `parseSentAt` отдавал
        `null`, а Prisma отличает `null` («записать NULL») от `undefined`
        («оставить дефолт `now()`»). Адаптер WebA машинного времени не видит и
        штатно шлёт `null` — вся его переписка ложилась в CRM без времени: в
        лентах (сортировка `nulls: "last"`) такие строки проваливаются в конец
        истории, в панели — наоборот всплывают наверх. Разбор переехал в
        `lib/ext/chat-identity.ts` (`parseMessageSentAt`) под тесты: из
        `route.ts` его не экспортировать, Next разрешает там только HTTP-методы.
        На проде нашлись 10 таких строк — тестовые, удалены со снимком.
      - **Блокер: ИИ-черновик обходил единственный тумблер согласия.** `buildAiDraft`
        собирал последние 10 сообщений и отправлял их на сервер и дальше
        провайдеру ИИ, не глядя на `logMessages` (заливка эту проверку делала).
        Решение заказчика: гейтить тем же тумблером — теперь панель не шлёт
        запрос вовсе и объясняет причину, service worker страхует вторым гардом,
        а подпись в настройках честно называет этот поток.
      - **Сортировка коммуникаций в панели** — `nulls: "last"`, как в лентах CRM.
      - **Филиальный scope расширения разошёлся с сессией:** `ext-auth` брал
        `employee_branches` для любой роли, а `lib/auth.ts` для владельца и
        управляющего всегда даёт «все филиалы». С висячими привязками (перевод
        из администратора, soft-delete филиала) владелец получал через панель
        «Клиент не найден» там, где в CRM видит всех.
      - **Блокировка за неоплату режет и `ext.ai`** — он стоит живых денег у
        провайдера; чтение карточки остаётся.
      - **Битое тело запроса** давало 500 без CORS-заголовков (панель видела
        «ошибку сети») — теперь 400 через `readExtJson`.
      - **Гонки панели.** Панель решает («залить переписку клиенту X», «вставить
        черновик») до похода на сервер, а исполняется это через секунды — за них
        человек успевает открыть другой диалог. Теперь панель передаёт ожидаемый
        `chatId`, service worker сверяет его с открытым чатом и отказывается
        писать/вставлять при расхождении (текст уходит в буфер обмена). Промах
        здесь необратим: ключ дедупа не даёт переписать чужие сообщения.
      - **Панель привязана к своему окну браузера** (`windowId`): боковая панель
        своя в каждом окне, а `lastFocusedWindow` отдавал вкладку окна «в
        фокусе» — панель окна A управляла вкладкой окна B.
      - **Переключение вкладки браузера стирало набранную задачу:** `tabs.onActivated`
        гнал полную перерисовку. Теперь при том же чате экран не трогаем; по ⟳
        перечитывание принудительное.
      - **Карточка поверх постороннего сайта:** `chatByTab` чистился только при
        закрытии вкладки. Добавлен `tabs.onUpdated` + проверка в `MSG_GET_STATE`.
      - **Белая панель при ошибке старта** (оба экрана скрыты в разметке, `#status`
        внутри скрытого `#main`) — `safeLoadState` показывает причину и «нажмите ⟳».
      - **Content script падал после обновления расширения:** `chrome.runtime.sendMessage`
        бросает СИНХРОННО («Extension context invalidated»), `.catch()` это не
        ловит. Отправка обёрнута, наблюдатель DOM отцепляется.
      - **Сбор сообщений сузился до контейнера активного диалога:** Telegram держит
        в DOM и другие открытые чаты, а ключ сообщения склеивается с `chatId`
        текущего — чужая переписка уезжала бы в карточку навсегда. Пустой
        активный чат подменять документом НЕЛЬЗЯ (это штатное состояние: первый
        диалог с родителем, чат из одних фото) — документ берём, только если ни
        один селектор контейнера не подошёл вовсе, то есть сломалась вёрстка.
      - **Порядок сообщений без времени.** У Telegram WebA времени взять неоткуда:
        `telegram-tt` проставляет подсказку `title` у `.message-time` только по
        наведению курсора, машинных атрибутов на пузыре нет. Поэтому сервер
        раскладывает такую пачку по позиции в ней (шаг в миллисекунду от момента
        заливки): пачка приходит хронологически, и общий `now()` на всех
        схлопнул бы порядок — в ленте строки встали бы как попало.
      - **Мелочи:** `insertIntoComposer` больше не дублирует текст при частичном
        успехе `execCommand` и честно возвращает результат; валюта берётся из
        организации, а не зашита рублём; поздний ответ поиска не перекрывает
        свежий; фоновое обновление раз в минуту больше не гонит батч на сервер.
      - **Тесты:** `parseMessageSentAt` и `ext-cors` (allow-list источников +
        разбор тела) — 783 юнит-теста приложения, 36 расширения.
      **Правки прошли адверсарное ревью** (32 агента, 14 подтверждённых находок),
      и половина из них была регрессом самой стабилизации — их тоже починили:
      гард «не перерисовывать, если чат тот же» сравнивал состояние вместо
      реально нарисованного чата и залипал после настроек; «Сохранить» в
      настройках оставляло карточку со старого сервера; у отрисовки не было
      поколения, из-за чего при быстром переключении диалогов на экране мог
      остаться клиент предыдущего чата, а заливка уходила по текущему — теперь
      есть `renderSeq`, снимок чата и `state.clientChatId` («из какого диалога
      взят показанный клиент»), и заливка/вставка/черновик привязаны именно к
      нему; поздний ответ поиска больше не дорисовывается в новый чат; сигнал
      активности несёт `windowId`; недописанная задача переживает перерисовку
      (черновик привязан к клиенту); нулевой баланс тоже показывается валютой
      организации.
- [x] **Каноникализация `chatId` в Telegram (31.08.2026).** Было: WebK (`/k`)
      пишет в адрес `@username`, WebA (`/a`) — всегда число, поэтому один и тот же
      собеседник давал два ключа: привязка из `/k` в `/a` не находилась, а
      переписка, залитая из обоих клиентов, задваивалась в карточке.

      **MAIN-world не понадобился, и прежнее утверждение об этом было неверным.**
      Числовой peer id лежит в ВИДИМОМ DOM: tweb сам проставляет `data-peer-id` на
      поле ввода (`input.ts`), на пузыре (`bubbles.ts` — там пир ДИАЛОГА, не
      отправителя), на аватаре шапки и на элементе списка диалогов. Обещание §7
      («только адресная строка и видимый DOM») остаётся в силе.

      Как устроено: адаптер больше не считает хэш идентификатором чата, а шлёт
      МЕШОК всего увиденного в одном диалоге в один момент (`chatId` + `altIds`);
      канон выбирает СЕРВЕР одной чистой функцией `chooseCanonicalChatId`. Правило
      разное по каналам (Telegram — число, MAX — сам chatId, WhatsApp Фазы 5 —
      пара «LID ↔ телефон»), и держать его в трёх адаптерах значило бы чинить одно
      и то же трижды.

      Привязка теперь группа строк `ChatBinding` с общим `canonicalChatId`
      (миграция аддитивная, колонка nullable). Резолв ищет по ВСЕМ
      идентификаторам и при находке достраивает каноническую строку — именно это
      делает привязку из `/k` находимой в `/a`, где `@username` в разметке не
      существует вовсе. Заливка читает ключ ДВОЙНЫМ способом (канон + алиасы):
      правка становится двусторонней дверью — откат кода не порождает дублей, а
      уже задвоенная история чинится лениво при открытии чата (переименование
      ключа, а не вставка: строка сохраняет время, автора и место в ленте).

      **Границы, зафиксированные тестами-замками.** Канонизируем ТОЛЬКО
      положительные числа — личные чаты. У групп, супергрупп и каналов арифметика
      клиентов расходится (WebK «-rawId», WebA «-(10^12 + rawId)»), а отличить
      базовую группу от супергруппы по одному числу нельзя: соблазн «дописать
      10^12» положил бы переписку одного чата в карточку другого. Идентификаторы
      одного чата, ведущие к РАЗНЫМ клиентам, дают `match: "conflict"` — выбирает
      человек, автоматически не сшиваем.

      Заодно закрыты соседние мины: гард «тот же чат?» сравнивал только `chatId`
      без канала (со вторым каналом два числовых id в разных вкладках могли
      совпасть); `#/im?p=<число>` отдавало число как peer id, хотя tweb трактует
      его как ЧАТ со знаком минус; попап предпросмотра (shift+click) подставлял
      чужой диалог в `messagesRoot`; неотправленные сообщения с временным дробным
      id заливались и ложились второй строкой.

      **Правки прошли адверсарное ревью** (105 агентов, 33 находки, каждую
      опровергали трое; подтвердились 17). Половина подтверждённого — регрессы
      самой каноникализации, и они починены: панель не слала `altIds` в резолв,
      из-за чего вся конструкция была мертва на главном пути; канон брался только
      из мешка запроса, а не из базы, поэтому при отказе чтения peer id ключ
      сообщений снова скакал; группа идентификаторов разворачивалась лишь «вниз»
      (от канона к участникам), из-за чего заход по нику не находил историю, а
      отвязка снимала одну строку из двух; перепривязка тащила переписку клиента
      из ЧУЖОГО филиала мимо `scopeClientByBranch`; штатное «Отвязать → привязать
      заново» теряло прежнего владельца, и перенос не срабатывал вовсе; гард
      «кадр перехода» верил одиночному наблюдению — теперь число из разметки
      требует подтверждения вторым тактом наблюдателя.

      **Проверка на живом Telegram обязательна** — см. `extension/TESTING.md`,
      раздел «Каноникализация chatId», пункт 32 (числа `/k` и `/a` обязаны
      совпасть до цифры). Всё, что известно о разметке, получено чтением
      исходников tweb и telegram-tt, а не живой проверкой.
- [ ] **Фаза 4 — MAX. Решение заказчика 28.08.2026: делаем, в полном объёме,
      включая заливку переписки.** Юридический риск и вероятность блокировки
      **приняты осознанно**; страховка — обкатка на СВОИХ аккаунтах до передачи
      функционала клиентам (см. «Условие выката» ниже). Спайк по бандлу
      `web.max.ru` от 28.08.2026 дал вот что.

      *Что уже готово в бэкенде и правок не требует:* канал `max` в
      `CommunicationChannel`, поле `Client.max`, `https://web.max.ru` в CORS
      allow-list (`lib/ext-cors.ts`).

      *Шаг 0 — мина обезврежена 31.08.2026.* Мин оказалось ДВЕ, обе вели к чужой
      переписке в карточке клиента.

      Первая — та, что описывал спайк: `normalizeChatId("max")` прогонял chatId
      через `phoneMatchKey` (последние 10 цифр у любой строки с ≥7 цифрами; у
      строки с буквами буквы выбрасывались целиком), а `resolve-client.ts` искал по
      обрезку клиента КАК ПО ТЕЛЕФОНУ и при единственном совпадении подставлял его
      без участия человека — панель тут же рисовала карточку и в фоне заливала туда
      переписку. Теперь chatId хранится как есть, а телефон принимается только
      отдельным явным полем `params.phone` (тракт для Telegram уже проложен);
      канал `max` убран из вывода телефона в резолве.

      Вторая, найденная аудитом и в спайке не описанная: общая обрезка пути по
      первому «/» превращала `web.max.ru/c/<chatId>/<messageId>` в **`c`**. ВСЕ
      групповые чаты и каналы схлопывались в один ключ, а уникальный индекс
      `(tenantId, channel, externalChatId)` склеивал их в одну привязку. Эта
      коллизия опаснее телефонной: она системная, а не случайная. Теперь путь MAX
      разбирается целиком — берём первый значащий сегмент, отбрасывая префиксы
      `c`, `u`, `join`, `joincall`, `stickerset`, `_storybook`; псевдо-маршруты
      состояния интерфейса (`/:chat-list`, `/:settings/...`) дают null, привязать
      их к клиенту нельзя.

      Граница зафиксирована тестами в `app/src/__tests__/ext-chat-identity.test.ts`
      (в т.ч. «две разные группы дают РАЗНЫЕ ключи»). Прежние тесты фиксировали
      как раз сломанное поведение и заменены.

      *Шаг 1 — probe на живом аккаунте: ЧАСТИЧНО ВЫПОЛНЕН 31.08.2026.*
      Инструмент — `extension/tools/max-probe.js` (вставляется в консоль, только
      читает DOM). Прогоны по четырём чатам одной организации.

      **Подтверждено фактом.**

      • *Личный чат* — один ЧИСЛОВОЙ сегмент: `web.max.ru/437719203`. Проверено
        на трёх разных диалогах.
      • *Групповой чат* — тоже ОДИН сегмент, но число ОТРИЦАТЕЛЬНОЕ:
        `web.max.ru/-78377804395205`. Формы `/c/<chatId>/<messageId>` у группы
        НЕТ — предположение спайка не подтвердилось (эта форма, судя по всему,
        относится к ссылке на сообщение в канале). Знак и есть признак группы —
        ровно как в Telegram.
      • *Список чатов* — просто `web.max.ru/`, пустой путь. Маршрута
        `/:chat-list` не существует; предположение спайка о псевдо-маршрутах с
        двоеточием живой проверкой НЕ подтверждено (обработка их безвредна и
        оставлена).
      • *Id сообщения в разметке НЕТ.* У узла только `role="presentation"` и
        `class`; вложенных `[id]`/`[data-id]`/`[data-message-id]` ноль во всех
        образцах, включая исходящие. Ключ дедупа придётся синтезировать.
      • *Машинного времени тоже нет.* Ни `title`, ни `datetime`; в `.meta` только
        часы («16:15»). Зато капсулы-разделители несут ПОЛНУЮ дату С ГОДОМ —
        «2 июля 2026». Это лучше, чем в Telegram: год не придётся угадывать.
      • *Направление* — класс `messageWrapper--isOut` (с заглавной O!) и,
        надёжнее, атрибут `data-bubbles-variant="incoming"|"outgoing"` ВНУТРИ
        пузыря. Осторожно: тот же атрибут стоит и внутри `composer`, поэтому
        искать его можно только в пределах `.messageWrapper`.
      • *Поле ввода* — `div.contenteditable` с `contenteditable=""` (ПУСТОЕ
        значение), `role="textbox"` и `data-lexical-editor="true"`. Это Lexical,
        редактор Meta. Два следствия: селектор `[contenteditable="true"]` его НЕ
        находит (наш `findComposer` для Telegram ищет именно так — для MAX не
        годится), и прямую правку DOM Lexical игнорирует или откатывает.
      • *Опор мало:* всего четыре `data-testid` на странице, полезен один —
        `composer`. Классы Svelte хешируются (`messageWrapper svelte-1kh0oxy`).
        Значит remote-конфиг селекторов для MAX действительно обязателен.

      **Найдено сверх плана: служебные строки ленты.** Звонки рисуются в общем
      списке текстом — «Отменённый вызов Видео 19:12», «Пропущенный вызов Аудио».
      Они НЕ пустые, поэтому наивный сбор утащил бы их в карточку клиента как
      реплики. Стикеры и фото такой опасности не несут: после вычистки `.meta` их
      текст пустеет, а пустые мы и так пропускаем.

      **Вставка текста проверена на живом MAX 31.08.2026 — способ выбран.**
      Оба способа редактор ПРИНИМАЕТ (кнопка отправки активна и там, и там), но
      различаются переносами строк:

      | Способ | Результат в поле | Перенос |
      |---|---|---|
      | `execCommand` (как в Telegram) | `строка одинстрока два` | ПОТЕРЯН |
      | синтетический `paste` | `строка один

строка два` | сохранён |

      Это ровно то, что предсказало исследование Lexical: `execCommand` не
      порождает `beforeinput`, поэтому `insertLineBreak` приходит с `data === null`,
      перенос не регистрируется, а вставленный браузером `<br>` сносит собственный
      MutationObserver редактора. **Значит адаптер MAX вставляет текст
      синтетическим `ClipboardEvent` с `DataTransfer`, а не `execCommand`** — и это
      расхождение с адаптером Telegram, где `execCommand` работает и менять его не
      надо.

      Две ловушки, найденные тем же прогоном:
      • У ПУСТОГО редактора Lexical `innerText` равен `
`, а не пустой строке (он
        рисует пустой абзац). Наивная проверка «что вставилось» через slice по
        длине прошлого текста съедает первый символ. Проверять успех надо
        асинхронно (Lexical реконсилирует DOM на микротаске) и по хвосту строки.
      • Двойной `
` означает, что вставка сделала АБЗАЦЫ, а не мягкие переносы:
        `innerText` склеивает соседние блочные узлы через `

`. На экране
        выглядит правильно (две строки), но НЕ проверено, что уходит в
        отправленном сообщении — там между строками может оказаться пустая.

      **MutationObserver ловит новое сообщение — проверено 31.08.2026.** Пришедшее
      сообщение поймано наблюдателем, значит сигнал активности для панели будет
      работать так же, как в Telegram. Заодно пятое подтверждение отсутствия
      идентификатора: у нового узла в атрибутах только `role` и `class`.

      **Осталось проверить руками:** уходит ли многострочная вставка ОДНИМ
      сообщением без лишней пустой строки (вставить и отправить в тестовый чат) —
      единственный открытый вопрос по вставке; виртуализируется ли список (первая
      проверка оказалась неинформативной: в чате из 14 сообщений подгружать сверху
      нечего, нужен чат с длинной историей — впрочем, для нас это второстепенно,
      мы и так берём только хвост ленты); стабилен ли chatId между сессиями.

      *Шаг 2 — адаптер `src/content/max.js`.* Детект чата — первый значащий
      сегмент `location.pathname` (разбор уже сделан на сервере в Шаге 0).
      **Групповые чаты (отрицательный id) панель обслуживать не должна:**
      привязка группы к клиенту не сценарий, а цена ошибки — вся групповая
      переписка в карточке одного человека, необратимо. Достаточно не предлагать
      привязку и не собирать сообщения, когда id отрицательный. **MAIN-world не
      нужен и не использовать:** CSP на `web.max.ru` — Report-Only с
      `report-uri`, то есть инъекция скрипта в страницу сама отправит отчёт на
      серверы MAX. `__oneme_auth` в localStorage не трогать категорически (§7).
      Смены чата ловить опросом `location.pathname`: SvelteKit ходит через
      `pushState`, `popstate` на него не срабатывает.

      *Шаг 3 — заливка переписки.* Ключ дедупа брать неоткуда: id сообщения в
      DOM нет. Синтезируем `externalId` из `chatId` + хеша «направление + время +
      текст» и принимаем два известных дефекта: правка сообщения даст вторую
      строку, а два одинаковых коротких сообщения в одну минуту схлопнутся в
      одну. Время — как в Telegram: часы из `.meta` + дата из капсул-разделителей
      (`Сегодня`/`Вчера`/`12 августа`), парсер по образцу
      `common/telegram-time.js` и под тесты; не разобрали — сервер разложит
      пачку по позиции.

      *Шаг 4 — remote JSON-конфиг селекторов* (§3). Для MAX он **обязателен, а не
      опционален**: Svelte 5 хеширует классы на каждой сборке (`svelte-gl41bh` и
      т.п.). Селекторы адаптера цепляются ТОЛЬКО за авторскую часть класса
      (`.messageWrapper`, `.message`, `.meta`, `.composer`, `.capsuleSeparator`)
      или за `data-testid`.

      *Условие выката (страховка вместо юридической):* сначала расширение
      работает в MAX **только на наших аккаунтах** — минимум неделю, с проверкой,
      не прилетает ли предупреждение или разлогин. В MAX один номер = один
      аккаунт, и практика блокировок по номеру уже есть (пока за Android
      мод-клиенты). Партнёрам канал MAX не включаем, пока эта обкатка не пройдена.

      *Параллельно и бесплатно:* бот-сценарий MAX закрывается серверно через
      Wazzup (там официальный Bot API, настоящие id и время) — вебхук у нас уже
      есть и починен в Фазе 1. Это не заменяет расширение (бот не видит переписку
      с личного номера администратора), но покрывает «родитель пишет в MAX
      центра» легально.
- [ ] **Фаза 5:** WhatsApp. **Фаза 6:** VK (официальный API сообществ отдаёт обе
      стороны диалога — там расширение вообще ничего не скрейпит).
- [ ] Юридический пакет и подготовка публикации — до пилота.

## 8.1. Следующий шаг (для новой сессии)

Состояние на 31.08.2026, конец дня. Фаза 4 (MAX) идёт: Шаг 0 и Шаг 1 закрыты,
Шаг 2 сделан наполовину.

### Что уже готово и проверено

- **Каноникализация `chatId` в Telegram** — сделана, задеплоена, ручной регресс
  по Telegram прогнан 31.08: ошибок нет. Подробности в §8.
- **Шаг 0 (мина MAX)** — обезврежен, задеплоен. Мин оказалось две, см. §8.
- **Шаг 1 (probe на живом MAX)** — закрыт. Все факты о разметке в §8; инструмент
  живёт в `extension/tools/max-probe.js` и годится для повторных прогонов.
- **Шаг 2, первая половина:**
  - `extension/src/common/max-time.js` — разбор времени MAX, 20 тестов;
  - `extension/src/content/adapter-core.js` — общий каркас адаптеров; `telegram.js`
    переведён на него без изменения поведения (регресс прогнан);
  - инжект в открытые вкладки сверяет `matches`, хосты выводятся из манифеста;
  - серверные гарды: групповые чаты MAX и белый список каналов для телефона.

### Что делать дальше — Шаг 2, вторая половина

Полный пофайловый план построен агентами 31.08 и лежит в истории сессии; ниже
выжимка, достаточная, чтобы начать.

1. `extension/src/common/max-path.js` — разбор адреса, чистый, под тесты.
   Правило: чат это ОДИН числовой сегмент; отрицательное число — группа (панель
   её не обслуживает); пустой путь — список чатов; всё прочее — не чат.
2. `extension/src/common/max-message.js` — детект служебных строк и синтетический
   ключ сообщения (id в разметке MAX нет вовсе). Ключ складывать из chatId,
   направления, времени и текста; версионный префикс обязателен, чтобы разовая
   чистка была возможна одним запросом.
3. `extension/src/content/max.js` — сам адаптер поверх `adapter-core.js`. Смену
   чата ловить опросом `location.pathname`: хэша у MAX нет, `popstate` не
   срабатывает. Вставка текста — синтетическим `ClipboardEvent`, НЕ `execCommand`
   (проверено вживую: `execCommand` склеивает строки).
4. Манифест: `web.max.ru` в `host_permissions`, вторая запись `content_scripts`
   (первым файлом — ядро), `web.max.ru` в `matches` у `web_accessible_resources`.
5. Панель: обработка группового чата отдельной причиной, текст «откройте
   web.telegram.org» сделать нейтральным.

### Гарды, которые нельзя ослаблять

- **Групповой чат MAX** — четыре рубежа: адаптер не отдаёт сообщений, service
  worker не заливает, панель показывает отдельную причину, сервер отклоняет в
  resolve/bindings/batch. Серверный убирать нельзя ни при каких условиях: он
  единственный переживает старую сборку расширения в браузере сотрудника.
  `DELETE` привязки НЕ гардим — иначе группа, привязанная старой сборкой, станет
  неудаляемой.
- **Нет разобранной даты → сообщение не заливаем.** Иначе ключ станет
  недетерминированным и карточка будет получать копию каждый день. Потеря
  обратима (зальётся при следующем открытии чата), дубль — нет.
- **Ни `data-bubbles-variant`, ни `--isOut` → пузырь пропускаем.** Правило «нет
  класса значит входящее» при переименовании модификатора молча превратит всю
  исходящую переписку во входящую.
- **Никогда не синтезировать `keydown` Enter** — это обработчик отправки, и
  Lexical не проверяет `isTrusted`. Нарушение = автоотправка, то есть отмена
  принципа-щита §3.
- **MAIN-world не использовать, `__oneme_auth` не трогать** (§7).

### Чего не хватает: probe v5

Часть решений принята осторожнее, чем нужно, просто потому что данных нет. Всё
закрывается одним прогоном на живом аккаунте:

- структурный признак служебной строки звонка (сейчас отсеиваем словарём —
  заведомо временная мера, любая новая формулировка MAX просочится в карточку);
- сколько миллисекунд пузыри прошлого чата живут в DOM после смены адреса — от
  этого зависят тайминги гарда «кадр перехода»;
- дословный вид модификатора направления в `classList`;
- полный набор форм текста капсулы на длинной истории («Вчера», дни недели);
- дерево пузыря с файлом и с фото С ПОДПИСЬЮ — что вычищать, чтобы подпись
  осталась, а «Скачать • 143.85 KB» ушло;
- виртуализируется ли лента (первая проверка была неинформативной: в чате из 14
  сообщений подгружать нечего). Для нас второстепенно — берём только хвост.

### Отложенное

- **Юрпакет и публикация** (§7) — шесть блокеров, ни один не начат: иконок
  расширения нет вовсе, политики конфиденциальности нет (`/offer` — это оферта
  SaaS), нет prominent disclosure при первом запуске, нет способа установки для
  не-разработчика (а справка CRM уже обещает ссылку), нечем дать доступ ревьюеру
  CWS, нет скрипта упаковки. Пока расширение живёт распакованным у нас — не
  блокирует; станет блокером, когда функционал пойдёт партнёрам.
- **Неделя на живой переписке** — рекомендация в силе: всё, что находилось до
  сих пор (склеенное время, «1 занятий», поиск по двум словам, `sent_at = NULL`,
  длинная ссылка за краем панели), вылезало на боевых данных, а не на тестах.
- **Выкат MAX партнёрам — только после недели обкатки на своих аккаунтах** (§8,
  «Условие выката»): смотреть, не прилетает ли предупреждение или разлогин.

Полезное для продолжения: проверять расширение автоматически можно только через
отдельный Chromium (`C:/Users/Cyberjinn/AppData/Local/Chromium/Application/chrome.exe`) —
системный Chrome игнорирует `--load-extension`. Вёрстку панели быстро проверяет
`cd app && node ../extension/tools/panel-preview.mjs`: он же ловит разъезжание
панели вбок на узкой ширине. Одноразовые скрипты проверки класть в `app/` (там
установлен Playwright) и удалять после прогона.
## 9. Работа с двух машин (десктоп ↔ ноут)

Задача длинная, параллельно идут другие задачи со второй машины. Режим:
**аддитивные изменения в `main`**, мелкие коммиты, частая синхронизация.

- Новое — новыми файлами; UI-точки входа включаем финальным коммитом; страница
  настроек скрыта; `extension/**` не входит в `paths` деплой-воркфлоу (коммиты
  туда деплой не запускают).
- Миграции **строго аддитивные** (новые таблицы, nullable-поля, новые значения
  enum) — накат на прод безвреден в любой момент.
- Горячие общие файлы (`prisma/schema.prisma`, `middleware.ts`,
  `page-help-content.ts`) — минимальные правки отдельным коммитом, сразу push.
- Ритуал: «Я ПРИШЁЛ» (`git pull --rebase`, при смене схемы — `prisma generate`),
  «Я УХОЖУ» (push даже при незаконченной фазе + проверка CI).
- Continuity — **этот документ** (раздел 8 «Фазы и статус») + память Claude, а не
  транскрипт чата: он машинно-локален и не синхронизируется.
