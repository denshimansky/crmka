# Аудит документации — Умная CRM v2.0

> **Дата:** 16.03.2026
> **Аудитор:** Claude (AI)
> **Источники:** PRD v2.1, data-dictionary v1.0, reports-logic, screens, questions, 15+ транскрипций встреч, 5 custdev-интервью
> **Цель:** Найти противоречия, пробелы, подводные камни и дать рекомендации перед стартом разработки

---

## Содержание

1. [Критические блокеры — исправить ДО начала разработки](#1-критические-блокеры)
2. [Противоречия между документами](#2-противоречия-между-документами)
3. [Пробелы в Data Dictionary](#3-пробелы-в-data-dictionary)
4. [Пробелы в PRD](#4-пробелы-в-prd)
5. [Отчёты: проблемы с данными и формулами](#5-отчёты-проблемы)
6. [Экраны: недостающие данные и UX](#6-экраны-проблемы)
7. [Мультитенант: риски утечки данных](#7-мультитенант)
8. [Потерянные решения со встреч](#8-потерянные-решения)
9. [Инсайты из custdev](#9-инсайты-custdev)
10. [Scope creep: что вынести из MVP](#10-scope-creep)
11. [Архитектурные риски](#11-архитектурные-риски)
12. [Баги 1С без описания исправления](#12-баги-1с)
13. [Дублирование отчётов](#13-дублирование-отчётов)
14. [Открытые вопросы без финального решения](#14-открытые-вопросы)
15. [Сводка и приоритеты](#15-сводка)

---

## 1. Критические блокеры

Эти проблемы ЛОМАЮТ ключевую логику. Исправить до начала кодирования.

### 1.1. `Attendance.subscription_id` обязательное — ломает пробные занятия

**Где:** data-dictionary, таблица `Attendance`
**Проблема:** Поле `subscription_id: UUID, да (обязательное)`. Но на пробном занятии абонемента нет. `TrialLesson` — отдельная сущность, но посещение пробного тоже фиксируется в `Attendance`.
**Решение:** Сделать `subscription_id` nullable. Добавить `trial_lesson_id: UUID, nullable, FK → TrialLesson`. Constraint: одно из двух должно быть заполнено.

### 1.2. `Payment.client_id` обязательное — ломает выемки и инкассации

**Где:** data-dictionary, таблица `Payment`
**Проблема:** PRD (US-023, FIN-08): "Приход, расход, выемки собственника, инкассации". `PaymentType` включает `owner_withdrawal | encashment`, но это не платёж клиента, а операция владельца. `client_id` обязательное — нельзя создать выемку.
**Решение:** Либо `client_id` nullable, либо отдельная таблица `AccountOperation` для внутренних операций.

### 1.3. `SubscriptionStatus` нет статуса "ожидание оплаты"

**Где:** data-dictionary, enum `SubscriptionStatus`
**Проблема:** PRD (US-004): "Абонемент создан, статус — Ожидание оплаты". Enum: `active | closed | withdrawn`. Нет `awaiting_payment` или `pending`.
**Решение:** Добавить `pending` в `SubscriptionStatus`.

### 1.4. Индивидуальное расписание (SCH-14) — нет модели данных

**Где:** PRD vs data-dictionary
**Проблема:** PRD (SCH-14): "Клиент ходит не на все дни группы (Пн+Ср — только Пн). Пересчёт абонемента автоматический". В DD нет ни одной таблицы/поля для хранения индивидуального расписания ученика в группе. `GroupEnrollment` не имеет выбранных дней.
**Решение:** Добавить таблицу `EnrollmentSchedule(enrollment_id, day_of_week)` или JSON-поле `selected_days` в `GroupEnrollment`.

### 1.5. `deleted_at` нигде не реализован

**Где:** data-dictionary, соглашения vs таблицы
**Проблема:** В соглашениях: "Soft delete — поле `deleted_at`". Но ни одна из 42 таблиц не содержит `deleted_at`. Soft delete невозможен.
**Решение:** Добавить `deleted_at: DateTime, nullable` на все сущности, подлежащие мягкому удалению (Client, Employee, Group, Subscription, Direction, Room, и т.д.). Определить список.

---

## 2. Противоречия между документами

### 2.1. Пробные занятия — двойная модель

**PRD (US-003):** Пробное может быть бесплатным или платным. При платном — стоимость засчитывается в абонемент.
**DD:** Есть `TrialLesson` (отдельная сущность) И `is_trial` в `Lesson` И `is_trial` в `Attendance` И `is_trial_credited` в `Subscription`. Но нет связи: как `TrialLesson.paid_amount` попадает в `Subscription.balance`? Нет `trial_lesson_id` в `Payment`.
**Рекомендация:** Убрать дублирование. Одна модель: `TrialLesson` хранит всё о пробном. `Attendance.trial_lesson_id` (nullable) для связи. `Payment.trial_lesson_id` (nullable) для оплаты пробного.

### 2.2. Копирование постоянных расходов (FIN-05)

**PRD:** "Копирование постоянных расходов с прошлого месяца".
**DD:** В `Expense` нет `is_recurring` или `source_expense_id`. `PlannedExpense` покрывает плановые, но PRD описывает копирование фактических.
**Рекомендация:** Добавить `is_recurring: Boolean` + `recurring_group_id: UUID` в `Expense`.

### 2.3. Объединение дубликатов (CRM-12) — неполный каскад

**PRD:** "Объединение дубликатов с превью".
**DD:** `ClientMergeLog` есть, но нет описания каскадного переноса: что с `Attendance`, `Payment`, `Subscription`, `CommunicationLog`? Все FK останутся на старом `client_id`.
**Рекомендация:** Описать миграцию FK при мерже. Или реализовать через обновление `client_id` во всех связанных таблицах + soft delete оригинала.

### 2.4. Баланс клиента — нет аудита

**PRD (US-004):** "Остаток при отчислении переносится на баланс клиента".
**DD:** `Client.client_balance` — одно число. Нет истории операций.
**Рекомендация:** Добавить `ClientBalanceTransaction(client_id, amount, type, subscription_id, description, created_at)`.

### 2.5. Загруженность центра (CRM-34) — нет данных о часах кабинета

**PRD:** "Факт часов / макс часов по кабинетам".
**DD:** `Room` имеет `capacity` (чел.), но нет рабочих часов кабинета. `Branch.working_hours_start/end` — это филиал, не кабинет.
**Рекомендация:** Наследовать часы кабинета от филиала (по умолчанию) или добавить `working_hours` в `Room`.

---

## 3. Пробелы в Data Dictionary

### 3.1. Отсутствующие таблицы

| Таблица | Зачем нужна | Ссылка в PRD |
|---|---|---|
| `EnrollmentSchedule` | Индивидуальное расписание ученика в группе | SCH-14 |
| `ClientBalanceTransaction` | Аудит движений по балансу клиента | US-004, FIN-18 |
| `ClientDocument` | Загрузка PDF (договоры, согласия) | screens 3.3 |
| `EmployeeDocument` | Загрузка PDF (паспорт, договор, СНИЛС) | screens 9.2 |
| `IntegrationSettings` | Хранение API-ключей интеграций (ЮKassa, Робокасса) | screens 13.8 |
| `AdminBonus` / `AdminBonusSettings` | Настройки мотивации администратора | SAL-12, screens 8.4 |

### 3.2. Отсутствующие поля

| Таблица | Поле | Зачем |
|---|---|---|
| `Expense` | `is_recurring`, `recurring_group_id` | FIN-05 копирование постоянных расходов |
| `Expense` / `ExpenseBranch` | `marketing_channel_id` | ROMI — привязка расхода к каналу |
| `Subscription` | `promised_payment_date` | Обещание оплаты на уровне абонемента, а не клиента |
| `GroupEnrollment` | `selected_days: Json` | Индивидуальное расписание (SCH-14) |
| `StockMovement` | тип `consumption` | Ежедневный расход канцтоваров (отчёт 8.2) |

### 3.3. Отсутствующие индексы

Для ключевых отчётов нужны:
- `GroupEnrollment(tenant_id, group_id, is_active)` — свободные места
- `TrialLesson(tenant_id, status, scheduled_date)` — отчёт пробников
- `Subscription(tenant_id, group_id)` — ученики в группе
- `Payment(tenant_id, date, method)` — оплаты по способам
- `SalaryAccrual(tenant_id, employee_id, period)` — агрегация ЗП по периоду

### 3.4. JSON-поля без описания структуры

- `Employee.custom_permissions` — нет схемы, нет валидации
- `CallCampaign.filter_criteria` — нет описания
- `Period.snapshot` — нет описания
- `DashboardWidget.settings` — нет описания

**Рекомендация:** Для каждого JSON-поля описать ожидаемую структуру в DD. Или заменить на структурированные таблицы (особенно `custom_permissions` → `RolePermission`).

---

## 4. Пробелы в PRD

### 4.1. Связанные скидки — нечёткая логика (SUB-07)

"При отчислении основания — пересчёт на будущее + уведомление". Что пересчитывается? Текущий абонемент? Следующий? Разница доначисляется? Пропорционально?

### 4.2. Лимит отрицательного баланса — нет описания действия (SUB-10)

DD: `NegativeBalanceAction: block_attendance | notify_only`. PRD не описывает: `block_attendance` блокирует отметку? Запись на занятия? Создание нового абонемента?

### 4.3. Возврат средств — нет процесса (FIN-18, SUB-11)

"Возврат: только полный остаток". Из какого счёта? Как влияет на ДДС? Абонемент закрывается автоматически? Что с непроведёнными занятиями?

### 4.4. Онлайн-оплата — нет edge cases (FIN-21)

Не описаны: таймаут/ошибка, webhook не пришёл, двойная оплата, автопривязка к абонементу.

### 4.5. Wizard онбординга — нет шагов (ADM-05)

Упомянут wizard + `onboarding_status` в DD, но нет описания: какие шаги, обязательные/опциональные, порядок.

### 4.6. Уведомления — канал не определён (DSH-06)

"Пустые группы, неотмеченные занятия, просроченные оплаты" — доставка через in-app? Push? Email?

### 4.7. Отработки — нет коэффициента

Custdev Семенюк (03.03): мини-сад 4 часа, при пропуске отработка 1:2. Ксения/Палевич подтвердила гибкий коэффициент. PRD (SCH-11) описывает только 1:1.
**Рекомендация:** Добавить настройку `makeup_ratio` на уровне направления или типа занятия.

---

## 5. Отчёты: проблемы с данными и формулами

### 5.1. Отчёты без данных в DD

| Отчёт | Проблема |
|---|---|
| **ROMI (7.8)** | Нет связи расход → маркетинговый канал. `Expense` не привязывается к `MarketingChannel` |
| **Мотивация админа (screens 8.4)** | Нет таблицы `AdminBonus` / `AdminBonusSettings` |
| **Эффективность обзвонов** | Нет формул в reports-logic. Метрики не определены |
| **Ожидаемые поступления** | Нет формулы расчёта. Прогноз из абонементов? из обещанных дат? |
| **Потенциальные сотрудники (10.7)** | Нет HR-таблицы в DD. Помечен v2, но описан без пометки |
| **Старт сезона (7.6)** | Нет сущности "предзапись". `WaitlistEntry` — v2 |

### 5.2. Неоднозначные формулы (деление на ноль, edge cases)

| Формула | Проблема | Рекомендация |
|---|---|---|
| **Средний чек (1.3)** | `Оплачено / Кол-во платежей` — если 0 платежей? | Показывать "—" или 0, описать в reports-logic |
| **Средний абонемент (1.4)** | 1 клиент пришёл 30 числа → средняя искажена | Нормализация или фильтр "минимум N занятий" |
| **Конверсия оттока по педагогам (2.2)** | 0 активных у педагога → деление на 0 | Не показывать педагогов с 0 активных |
| **Отток по месяцам (2.3)** | `sale_date = null` → клиент выпадает | Fallback: дата первого платежа или создания |
| **Финрез — распределение расходов (7.2)** | Доход всех направлений = 0 → невозможно | Fallback: равные доли или по кол-ву учеников |
| **Прибыльность педагога (7.4)** | "Средние расходы" — за какой период? | Явно указать: текущий месяц, данный филиал |
| **Активные абонементы (1.1)** | "Активность на конец месяца" — нечёткое определение | Формализовать: наличие баланса > 0 AND статус active |
| **LTV (1.6)** | "Без учёта скидок и корректировок" — скидки в Subscription, не в Payment | Уточнить: LTV = сумма Payment.amount (уже со скидкой) |

---

## 6. Экраны: недостающие данные и UX

### 6.1. Экраны без данных в DD

| Экран | Проблема |
|---|---|
| **/clients/:id — Документы** | Нет таблицы `ClientDocument` |
| **/staff/:id — Документы** | Нет таблицы `EmployeeDocument` |
| **/settings/integrations** | Нет `IntegrationSettings` — где хранить API-ключи? |
| **/dashboard — Виджет "Маржа"** | Маржа = ? (выручка - переменные? - все расходы?) Не определено |
| **/dashboard — Виджет "Оборот"** | Отличие "оборот" от "выручка" не формализовано |
| **/attendance — типы дней** | `Attendance.type` — строка, не FK на справочник |

### 6.2. UX-проблемы

**Критичные:**
- **Нет empty states.** Ни один экран не описывает состояние загрузки, ошибки, пустых данных. Для нового партнёра (первый вход) каждый экран будет пустым — нужны подсказки.
- **Нет экрана "Закрытие месяца".** Финрез формируется после закрытия (Period), но UI для управления периодами не описан.
- **Нет валидации конфликтов расписания.** 2 группы в 1 кабинете, 1 инструктор в 2 группах — не описано.

**Средние:**
- **Каталог отчётов — 40+ штук на одной странице.** Нет поиска, избранного, рекомендованных. Нужна фильтрация.
- **Воронка — нет лимитов Kanban.** При 500+ лидах станет нечитаемой. Нужен fallback на таблицу.
- **Мобильная адаптация отчётов.** Отчёты с 8-10 столбцами (финрез, зарплата) не влезут в карточки. Нужно конкретное решение.
- **Кабинет клиента — бедный функционал.** Нет: сообщить о пропуске, записаться на отработку, история посещений.

---

## 7. Мультитенант: риски утечки данных

### 7.1. Справочники с `tenant_id = null` (системные)

`AttendanceType`, `ExpenseCategory`, `WithdrawalReason`, `AbsenceReason`, `MarketingChannel` — все имеют `tenant_id = null` для системных записей. RLS-политика должна возвращать `WHERE tenant_id = current ИЛИ tenant_id IS NULL`.

**Риск:** Одна ошибка — и пользовательские справочники одного тенанта видны другому.
**Рекомендация:** Тесты на изоляцию тенантов для каждого справочника. Или отдельная таблица `SystemDefault*` без tenant_id.

### 7.2. `ClientPortalUser` — один человек, два тенанта

Авторизация через телефон/email. Если родитель — клиент в двух организациях, будет два `ClientPortalUser`. При логине система должна правильно выбрать тенант.
**Рекомендация:** Экран выбора организации после логина (как в Slack).

### 7.3. `Invoice` и `SupportTicket` — без tenant_id

Используют `organization_id` вместо `tenant_id`. RLS-политики нужно писать отдельно.

### 7.4. Все остальные 30+ таблиц — ОК, tenant_id присутствует.

---

## 8. Потерянные решения со встреч

Решения, принятые на встречах, но НЕ отражённые в PRD.

### 8.1. Отработки с коэффициентом 1:2

**Источник:** Custdev Семенюк (03.03), Ксения/Палевич (11.03)
**Суть:** Мини-сад 4 часа стоит дороже. При пропуске отработка 1:2 (одно пропущенное = два отработанных).
**Статус в PRD:** Не отражено. SCH-11 описывает только 1:1.

### 8.2. Интеграция с Wazzup (мессенджер-агрегатор)

**Источник:** Встреча 09.03
**Суть:** Анна: Чернова до сих пор платит за Wazzup. Денис предлагает интеграцию.
**Статус в PRD:** Мессенджеры отложены на v3 (2027). Wazzup не упоминается.

### 8.3. QR-код (СБП) как способ оплаты

**Источник:** Custdev Лера (10.03)
**Суть:** "Мы эквайринг на QR-код заменили, он дешевле".
**Статус в PRD:** FIN-01 перечисляет наличные/безнал/эквайринг/онлайн. СБП/QR не упоминается.

### 8.4. Автоматический дайджест руководителю

**Источник:** Custdev Виктория, Настя С.
**Суть:** Каждый вечер вручную собирают отчёт (актив, абонементы, непродлённые, отток).
**Статус в PRD:** Нет user story на автоматический дайджест.

### 8.5. Рассылка счетов/ссылок на оплату клиентам

**Источник:** Custdev Виктория, Настя, Лера
**Суть:** "Письма счастья" — рассылка счетов через Telegram/SMS.
**Статус в PRD:** Не описан механизм отправки счёта клиенту.

### 8.6. NPS клиентов

**Источник:** Встреча 09.03 (Анна: "Оля собирала NPS")
**Статус в PRD:** Нет ничего про NPS или опросы.

### 8.7. Расписание для педагогов — упрощённый/печатный вид

**Источник:** Ксения/Палевич (11.03), Лера (10.03)
**Суть:** Педагоги не работают в CRM, им дублируют расписание в документах.
**Статус в PRD:** Роль инструктора описана, но нет упрощённого вида.

---

## 9. Инсайты из custdev

### 9.1. Собственники НЕ пользуются финансовой аналитикой

Семенюк: "финансовые штуки понравились, но я до сих пор ничем не пользуюсь". Лера: финрез не закрывает.
**Вывод:** Финансовый модуль ценен, но ТОЛЬКО если данные корректно вносятся. Нужен максимально простой UX для ввода расходов.

### 9.2. Решения принимаются по воронке, не по финансам

Лера: рост = "лиды → пробники → покупки + иногда отток по педагогам".
**Вывод:** Воронка и конверсии — приоритет №1 для UX. Финрез — "nice to have" для продвинутых.

### 9.3. AlphaCRM проигрывает в поддержке

Семенюк: "AlphaCRM такие большие, им вообще пофигу".
**Вывод:** Человеческая поддержка — конкурентное преимущество. Сохранять.

### 9.4. Педагоги вообще не работают в CRM

Ксения/Палевич и Лера подтвердили: педагоги не заходят, посещаемость отмечают админы.
**Вывод:** Роль инструктора в MVP может быть минимальной (только расчётный листок).

### 9.5. Lock-in через CRM слабый

Семенюк: если CRM пропадёт, "болезненно, но можно перенести".
**Вывод:** Создавать ценность, которую сложно перенести: аналитика трендов, автоматизация.

### 9.6. Справки задним числом ломают зарплату

PRD (SAL-11b) описывает корректировки — ОК, но нужно сделать максимально наглядно.

---

## 10. Scope creep: что вынести из MVP

MVP содержит слишком много для команды из 2-3 разработчиков с дедлайном 1 июня 2026.

### Рекомендация: вынести в v1.1

| Что | Почему | Ссылка |
|---|---|---|
| **14 аналитических отчётов (CRM-25..38)** | Не блокируют операционную работу | PRD раздел 14 |
| **Модуль обзвона (CALL-01..04)** | Для 20 клиентов — overhead | PRD 7.19 |
| **Склад (INV-01..06)** | Сложная подсистема. "Тетрадка" работает | PRD 7.16 |
| **Настраиваемый дашборд (DSH-01)** | Drag-and-drop. Хватит фиксированного | PRD 7.6 |
| **Личный кабинет клиента (раздел 12.2)** | Отдельный фронтенд | PRD 12.2 |
| **Объединение дубликатов (CRM-12)** | Каскадный merge — сложно | PRD 7.4 |
| **Справка на каждой странице (FIN-33)** | Большой объём контента | PRD 7.14 |

**Эффект:** Сокращение scope на ~30-35%. Фокус на ядро: расписание, посещения, абонементы, оплаты, базовые отчёты, воронка.

---

## 11. Архитектурные риски

### 11.1. Каскад при отметке посещения

Одна отметка: `Attendance` → `Subscription.balance` → `SalaryAccrual` → `Client.client_balance`. Массовая отметка (15 учеников) = 60 операций.
**Рекомендация:** Транзакционная стратегия + очередь для начислений ЗП (async).

### 11.2. RLS + сложные JOIN = медленные отчёты

"Прибыльность педагога": 5 таблиц с RLS на каждой.
**Рекомендация:** Materialized views или денормализованные таблицы для отчётов.

### 11.3. Денормализованные балансы без reconciliation

`Account.balance`, `Client.client_balance`, `Subscription.balance` — любой баг ломает баланс.
**Рекомендация:** Фоновый job пересчёта. Отчёт "Сверка" для админа.

### 11.4. NextAuth.js + кастомная Employee — непонятная связь

NextAuth создаёт свои таблицы. DD описывает `Employee.password_hash`. Как связаны?
**Рекомендация:** Определить: NextAuth с кастомным adapter → Employee, или свой auth.

### 11.5. Два статуса Client — риск рассинхронизации

`funnel_status` и `client_status` — параллельные. Нет описания триггера перехода.
**Рекомендация:** Описать state machine с триггерами.

### 11.6. Production Calendar per tenant — overhead

Каждый из 500 партнёров будет иметь свою копию календаря. 95% — один и тот же.
**Рекомендация:** Системный календарь + пользовательские исключения.

---

## 12. Баги 1С без описания исправления

| Баг | Описание | Решение в новой системе |
|---|---|---|
| **Нет даты продажи (2.3)** | Клиент без sale_date выпадает из отчёта оттока | ❌ Не описано. Fallback: дата первого платежа |
| **Прогул = отработанное (6.3)** | Все ученики прогуляли — занятие "отработано" | ❌ Не описано. Связано с `pay_instructor_for_absence` |
| **Новые не попадают (7.5)** | Продажа в одном месяце, занятие в другом | ❌ Не описано. Считать по дате занятия? |
| **Конечный остаток склада (8.2)** | Не совпадает с "Остатки канцтоваров" | ⚠️ Нужен единый источник данных |
| **Оплата после закрытия (5.8)** | Оплата мая 5 июня → должник на 31.05 | ⚠️ Бизнес-правило, не баг. Уточнить |
| **Премии не в прибыльности (7.4)** | ЗП = только начисления, без премий/штрафов | ⚠️ Включить SalaryAdjustment в формулу |

---

## 13. Дублирование отчётов

7 пар можно объединить, сократив разработку:

| Вместо двух отчётов | Один с переключателем |
|---|---|
| Активные абонементы (1.1) + Сводный по педагогам (7.5) | Группировка: филиал / педагог / общий |
| Отток по педагогам (2.2) + Отток по направлениям (2.4) | Группировка: педагог / направление |
| Финрез (7.2) + % распределения (7.3) | Один отчёт + переключатель "показать %" |
| ЗП педагогов (6.1) + Расчёты с педагогами (10.6) | Вид: руководитель / бухгалтер |
| Остатки канцтоваров (8.1) + Остатки (screens 10.1) | Один и тот же |
| Детализация оттока (2.1) + Отток по направлениям (2.4) | Drill-down одного отчёта |
| Поступления по дням (5.7) + ДДС (5.5) | Drill-down из ДДС |

**Эффект:** Минус 7 отчётов из ~58 при том же функционале.

---

## 14. Открытые вопросы без финального решения

| # | Вопрос | Статус | Рекомендация |
|---|---|---|---|
| 1 | Рентабельность групп — какие расходы? | Не решён | P&L на уровне группы: прямые + доля общих |
| 7 | Критерии сегментации — по абонементам или все три? | Не решён | По абонементам для MVP |
| 9 | Финрез: live или после закрытия? | Обсуждался | Live с пометкой "месяц не закрыт" |
| 10 | Статьи затрат — достаточно ли привязки к направлению? | Не решён | Достаточно для MVP |
| 13 | Юрлицо — ИП или новое? | Не решён | Не блокирует, но нужно для счетов |
| — | Ценообразование SaaS | PRD: 5000₽/филиал | Уточнить с Анной |
| — | Кто закрывает финрез? | Роль "бухгалтер" убрана | Владелец или управляющий? |
| — | Custdev 5 клиентов не проведён | Фирова, Чернова и др. | Полезно для валидации |

---

## 15. Сводка и приоритеты

### Статистика находок

| Категория | Критичных | Средних | Инфо |
|---|---|---|---|
| Блокеры DD (раздел 1) | **5** | — | — |
| Противоречия (раздел 2) | 2 | 3 | — |
| Пробелы DD (раздел 3) | 3 | 5 | 4 |
| Пробелы PRD (раздел 4) | 2 | 5 | — |
| Отчёты (раздел 5) | 3 | 8 | — |
| Экраны/UX (раздел 6) | 3 | 5 | 3 |
| Мультитенант (раздел 7) | 1 | 2 | 1 |
| Потерянные решения (раздел 8) | — | 7 | — |
| Scope creep (раздел 10) | — | 7 | — |
| Архитектурные риски (раздел 11) | 2 | 4 | — |
| Баги 1С (раздел 12) | 3 | 3 | — |
| **Итого** | **24** | **49** | **8** |

### Порядок действий

**Неделя 1 (до начала кодирования):**
1. Исправить 5 критических блокеров в DD (раздел 1)
2. Определить scope MVP — вынести рекомендованное в v1.1 (раздел 10)
3. Закрыть open questions (раздел 14)
4. Описать edge cases для ключевых формул (раздел 5.2)

**Неделя 2 (параллельно со стартом):**
5. Добавить недостающие таблицы в DD (раздел 3.1)
6. Описать empty states для ключевых экранов (раздел 6.2)
7. Определить auth-стратегию: NextAuth adapter или свой (раздел 11.4)

**По ходу разработки:**
8. Объединить дублирующиеся отчёты (раздел 13)
9. Закрыть потерянные решения со встреч (раздел 8)
10. Внедрить reconciliation для балансов (раздел 11.3)
